name: Build VoxMorph

on:
  push:
  workflow_dispatch:

jobs:
  build-macos:
    runs-on: macos-14
    steps:
      - uses: actions/checkout@v6

      - name: Configure (Universal binary)
        run: cmake -B build -DCMAKE_BUILD_TYPE=Release -DCMAKE_OSX_ARCHITECTURES="arm64;x86_64"

      - name: Build
        run: cmake --build build --parallel 3

      - name: Ad-hoc codesign
        run: |
          codesign --force --deep -s - "build/VoxMorph_artefacts/Release/AU/VoxMorph.component"
          codesign --force --deep -s - "build/VoxMorph_artefacts/Release/VST3/VoxMorph.vst3"
          codesign --force --deep -s - "build/VoxMorph_artefacts/Release/Standalone/VoxMorph.app"

      - name: Build installer package (.pkg)
        run: |
          STAGE=pkgroot
          mkdir -p "$STAGE/Library/Audio/Plug-Ins/Components" "$STAGE/Library/Audio/Plug-Ins/VST3" "$STAGE/Applications"
          cp -R "build/VoxMorph_artefacts/Release/AU/VoxMorph.component" "$STAGE/Library/Audio/Plug-Ins/Components/"
          cp -R "build/VoxMorph_artefacts/Release/VST3/VoxMorph.vst3" "$STAGE/Library/Audio/Plug-Ins/VST3/"
          cp -R "build/VoxMorph_artefacts/Release/Standalone/VoxMorph.app" "$STAGE/Applications/"

          # disable bundle relocation: always install to the fixed paths,
          # never "update" a stray old copy found somewhere else
          pkgbuild --analyze --root "$STAGE" components.plist
          python3 - <<'EOF'
          import plistlib
          with open('components.plist','rb') as f:
              comps = plistlib.load(f)
          for c in comps:
              c['BundleIsRelocatable'] = False
          with open('components.plist','wb') as f:
              plistlib.dump(comps, f)
          EOF

          # preinstall script removes old v0.1 copies (incl. per-user installs)
          mkdir -p pkgscripts
          cp scripts/preinstall pkgscripts/preinstall
          chmod +x pkgscripts/preinstall

          # Taken from CMakeLists rather than written out again: this line
          # had been bumped by hand with every release since v0.63.x and was
          # missed for v0.67.0, so the installer claimed 0.66.0 for a 0.67.0
          # build. macOS compares this version when deciding whether a
          # package is an upgrade, so a stale one is not cosmetic.
          VM_VERSION=$(sed -n 's/^project(VoxMorph VERSION \([0-9.]*\)).*/\1/p' CMakeLists.txt)
          test -n "$VM_VERSION"          # fail loudly rather than ship "--version"
          echo "installer version: $VM_VERSION"

          pkgbuild --root "$STAGE" \
                   --component-plist components.plist \
                   --scripts pkgscripts \
                   --identifier com.hakamaaudio.voxmorph \
                   --version "$VM_VERSION" \
                   "VoxMorph-Installer.pkg"

      - uses: actions/upload-artifact@v6
        with:
          name: VoxMorph-macOS-Installer
          path: VoxMorph-Installer.pkg

  build-windows:
    runs-on: windows-2022
    steps:
      - uses: actions/checkout@v6

      - name: Configure
        run: cmake -B build

      - name: Build
        run: cmake --build build --config Release --parallel 3

      - uses: actions/upload-artifact@v6
        with:
          name: VoxMorph-Windows
          path: |
            build/VoxMorph_artefacts/Release/VST3
            build/VoxMorph_artefacts/Release/Standalone
