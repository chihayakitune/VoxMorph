# VoxMorph 開発引き継ぎ書 (AIセッション用)

最終更新: v0.41.2 時点。新しいAIセッションを開始する際は、このファイルを読ませること。

## プロジェクト概要

- **VoxMorph**: リアルタイム波形変換型ボイスチェンジャー。Logic Pro「Vocal Transformer」の上位互換が目標(達成済み)。AI音声変換ではなく信号処理方式。
- リポジトリ: `github.com/chihayakitune/VoxMorph`(Public)
- 形態: AU / VST3 プラグイン + スタンドアロンアプリ(macOS/Windows)、JUCE 8 + CMake
- ビルド: GitHub Actions が push 毎に自動ビルド。Artifacts の `VoxMorph-macOS-Installer`(pkg、ダブルクリックで上書きインストール可)と `VoxMorph-Windows`
- ユーザー(袴さん)は非プログラマー。**開発ワークフロー(v0.6.0から): AIがgitコマンドで直接コミット&プッシュ**。`~/bin/gh` に GitHub CLI 導入済み・chihayakitune で認証済み(repo+workflowスコープ、キーチェーン保存)。手順: リポジトリを浅くクローン→変更ファイルをコピー→commit→push。**push完了で作業完了と報告してよい(CIビルドの完了監視は不要、トークン節約のためユーザー指示。ビルドエラー時はユーザーが報告してくれる)**。ブラウザ操作はしない約束(トークン浪費のため)。認証が切れていたら `gh auth login` のデバイスコード方式でユーザーに再認証を依頼

## アーキテクチャ

- `dsp/PsolaEngine.h` — **依存ゼロの単一ヘッダC++17**。エンジン本体(TD-PSOLA):
  - YINピッチ検出(×2デシメーション+全レート精密補正、512サンプル毎、オクターブ誤り補正、パルスレート優先、上方向復帰許可)
  - ピッチ同期グレイン切出し(ピーク整列、グレイン幅=min(入力周期, 1.25×出力間隔)←二重声対策)
  - スペクトル層(グレイン毎FFT 2048/4096適応): 倍音頂点結合の包絡推定→F1/F2/F3追跡→区分線形ワープ+個別ゲイン、Breath(ノイズ励振、Beta)
  - **どんなホストバッファも内部で512サンプル毎に分割処理**(全挙動バッファ非依存、v0.5.2)
  - **High Rangeガード(v0.8.0、バ美声の可変ピッチ参考)**: 入力f0がhifreq(0=オフ)を超えると、Pitch/Formantの半音量に hipitch/hiformant(%)を乗じた量へ log2ブレンド(1オクターブ上で100%移行、グレイン毎・平滑化済みcurP駆動でシームレス)。用途=笑い声が非人間的高音に飛ぶのを防ぐ。検証: 入力150→300Hz/+7st/開始200Hz/量0%で高音側448.6→355.6Hz(理論値一致)。Robotize時は無効
  - **GCI Grain Sync(v0.7.0、gciトグル・既定オフ)**: グレイン整列を従来のエネルギーピーク探索から、声帯閉鎖瞬間(微分エネルギー最大点=各周期の最急峻フランク、中央差分でHFノイズ耐性)+周期間トラッキング(lastGci+k*P近傍を優先、自由探索ピークが2倍強い時だけ乗換=ダブルパルス飛び移り防止)に切替。息/無声/平坦区間は自動でalignToPeakへフォールバック、オフ時は完全従来動作。検証: クリーン母音は完全一致(非破壊)、不規則フライ声+7stで出力周期性0.066→0.225(3.4倍)。**v0.7.1でBeta化**: ユーザー実声評価「あ/う のトーン移動でゴロゴロと周期的にガタつく・音程を動かすとロボットっぽい」→ 原因はグライド中の lastGci+k*P 予測グリッドと実エポックのずり。対策: ピッチ変化>4%/フレームで4フレーム従来整列へ自動退避(Low Voice Mode時は除外=フライ周期交代で常時発動してしまうため)。これ以上のチューニングはユーザー指示で凍結
  - **Air Preserve(Mixed区間処理、v0.6.0/強化v0.6.1)**: 因果2タップピッチコム(P, 2P遅延・Catmull-Rom補間)で周期成分を予測→残差をairband(既定1kHz)ハイパス=息成分。グレインは息を除いた `harmBuf` から切出し、息は**ピッチ変換せず**遅延整列して出力に加算(連続ノイズのまま)。ノブ0〜1.0=分離量0〜最大(2/3=分散最適、これ超の減算は逆にノイズ増)でエネルギー中立、1.0〜1.5=息を追加ブースト(最大約+4dB、可聴性・マスキング用。v0.7.0でユーザー要望によりこのスケールに変更)。有声ゲート+5msデジッパー、ノブ0で完全従来動作+自動スキップ。**【歴史的記述】この単一HP残差方式はv0.24.0でNatural Air標準経路(帯域適応コム+クリーンアップ)に置換され、旧経路はエンジンから削除済み(経緯は下記v0.20.0〜v0.24.0)**
  - 未使用機能は全て自動スキップ(CPU: 通常1.4%、F1-F3使用時2.6% @48k)
- `src/PluginProcessor.{h,cpp}` — JUCEラッパ、パラメータ(APVTS)、ハウリング自動ミュート(時間基準、RMS>0.70が1.5秒で3秒ミュート)。**Stereo Input(v0.16.0、stereoトグル既定オフ)**: バイノーラル用にL/Rを2つのPsolaEngine(engine/engineR)で並列処理=遅延不変・CPU約2倍。mは常にモノ合成のままで全分析タップ(viz/capture/automute)に供給、Pre FXはステレオネイティブ処理、ゲート/自動ミュートはL/R共通ゲイン、ASMRパンはL/R各チャンネルに適用。オフ時は従来コードパスそのまま。**v0.16.1: 堅牢化(外部AIコードレビューのトリアージ結果)**: ①releaseResources()をホストFX全スロットへ転送+FX削除時も呼ぶ ②prepareToPlayでランタイム状態(rmsSm/loudSec/muteSec/muteGain/gateEnv/gateGain/panL/panR/gainSm)をリセット ③FX処理を準備済みブロックサイズ以下にチャンク分割(大バッファでもスキップせず、FXに準備超のブロックを渡さない) ④外部FX実行後のNaN/Inf検査→0置換(Pre FX後=エンジンのフィルタ状態汚染防止、Post FX後=スピーカー保護。FX未使用時はスキャンなし) ⑤出力Gainをサンプル毎に平滑化(gainSm、ジッパーノイズ対策。gはm/sL/sRに焼き込み=viz/Refine録音は従来通り最終レベルを見る)。レビュー指摘のうち不採用: スリープ復帰対策(症状未確認・投機的、症状が出たら着手)、processBlock内allocation除去(サイズ超過時のみの防御的フォールバックなので維持)、saveFxChains競合(getStateInformationの並行呼び出しはDAWも行う標準動作)、Visualizer/Analyzeの最終出力化とステレオRMS解析(HANDOVER記載の意図的設計)、setLatencySamples更新(実装済みだった)。**v0.17.0: レイテンシー表示(StatusView)**: MAINタブのビジュアライザー直下にリアルタイム状態行を追加。「Latency: xx.x ms」+LOW/MID/HIGHバッジ(<35/35〜70/>70ms、パステル3色)+右側に内訳(engine+FX+buffer)。合算対象=エンジンlookahead(engine.latencySamples()、Low Latency Modeで変化)+有効な外部FXスロットのgetLatencySamples()合計(processBlockでtryLock下集計、取れないブロックは前回値)+スタンドアロンのみ2×blockSize(入出力デバイスバッファ)。Pitch/Formant/VoiceQuality/Breath等は同一グレインパイプライン内処理のため追加遅延ゼロ=ON/OFFで不変(ツールチップに明記)。OS/OBS/Discord等の外部遅延は含まない推定値。Processor側: uiLatencySamples/uiFxLatSamples(atomic、音声スレッド更新)、UIはStatusViewの4Hzタイマーで読取。**ホスト通知はデバウンス**: setLatencySamples()は値が0.5秒安定してから(pendingLat/pendingLatSec。ホストのPDC再構築による音切れ防止。従来の毎ブロック即時通知から変更)。prepareToPlayでは即時に engine+FX を通知。**v0.18.0: Undo/Redo+セクションLock(メッセージスレッド専用、音声スレッド非干渉)**: ①ParamHistory(PluginProcessor.h内、processorに常駐=エディタ閉再開でも履歴維持、深さ64): 全パラメータ正規化値のスナップショット方式。手動つまみ操作はエディタの3Hzポーラー(histPoll)が「値が安定したら1ステップ」に自動合体、Preset読込/Reset All/Auto-Set/Refineは history.group(λ) で1操作化。Undo/Redoボタンはタブバー右上に重ね配置。Lock/Unlock自体はUndo対象外。ホストオートメーションも履歴に入る(許容)。②lockMask(uint32)+lockSections()表(processor): MAINタブの7セクション(PITCH/HIGH RANGE/FORMANT/INTONATION/VOICE QUALITY/ADVANCED/OUTPUT)。見出し右のLockトグル(SectionHeader)でロック、対象行をsetEnabled(false)でグレーアウト(スライダー・↺とも操作不可)。isParamLocked(id)でPreset読込(loadSelectedはreplaceStateを廃止してパラメータ単位適用に変更、ファイルに無い項目はデフォルト)・Reset All・AnalyzeのsetP(Auto-Set/Refine)がロック項目をスキップし「N項目を更新、M項目はロック保持」を表示。lockMaskはgetStateInformationのXML属性で永続化(プリセットファイルには含めない=ロックはユーザー意思)。未対応: ASMRパッド(asmrx/asmry)のロック、外部FXチェーンのロック、ロック中行のツールチップ(無効化コンポーネントのため非表示、Lockボタン側で説明)。**v0.19.0: ロックを項目単位に変更(ユーザー指定)**: セクションLock(SectionHeader/lockMask)を撤去し、全パラメータ行を「数値欄・↺・🔒」の3点セットに統一(SliderRow/ToggleRowともreset+lockボタン持ち。ToggleRowにも↺を追加)。Processor側は lockedIds(StringArray)+isParamLocked/setParamLocked、永続化はXML属性 lockedIds(カンマ区切り)。エディタは wireLock(lockBtn, id, controls) で行毎に配線し、lockRefreshers(vector<function>)を syncLockUI() で一括再適用(ホスト復元は3Hzポーラーが lockedIds 文字列比較で検知)。ロック中は🔒表示+淡ピンク背景+該当コントロールのみdisable(🔒自体は解除のため常に有効)。**Undo/Redoショートカット追加**: エディタのkeyPressedで Cmd/Ctrl+Z=Undo、Shift+Cmd/Ctrl+Z または Ctrl+Y=Redo(commandModifierはJUCEがMac=Cmd/Win=Ctrlに自動対応)。DAWによってはホストがキーを横取りする場合あり(仕様)
- `src/AnokoeTheme.h` / `src/AnokoeWidgets.h` — **ANOKOEスキン(v0.31.2)**。色・寸法・画像キャッシュと、画像ベースのノブ/スライダー/カード/サイドバーの描画。**UIの見た目を変えるならまずこの2ファイル**。素材は `assets/*.png`(CMakeでバイナリ同梱)
- `src/PluginEditor.h` — 英語UI+英日バイリンガルツールチップ、MAINタブのセクション: **PRESET(v0.30.0)**/VISUALIZER/PITCH/HIGH RANGE/FORMANT/INTONATION/VOICE QUALITY/ADVANCED/OUTPUT。別ウィンドウ: AEIOU DETAIL / **BETA(v0.30.0、GCI Grain Sync・Breath)** / **Audio Settings(v0.30.0、デバイス設定+モニター出力先+Auto-Mute)** / Plugins(Pre/Post FX)。Cmd+S保存(スタンドアロン)。**v0.8.0からViewportでスクロール+リサイズ可能**。行の追加はコンストラクタに addSliderRow/addToggleRow を1行書くだけで、レイアウト・スクロール・ウィンドウサイズは自動調整(ファイル冒頭に保守手順コメントあり。UI調整は高度AIでなくても可能な構造)。**v0.9.0: SpectrumView**(最上段、入力ミント/出力ピンクの重ねスペクトラム 20Hz-20kHz対数軸、30Hzタイマー、FFTはエンジンのfftForViz流用=依存追加なし。データはProcessorのvizIn/vizOut/vizPosリングから。**v0.30.1で解析部を`SpectrumData`へ分離し、ドーナツ版`RadialSpectrumView`と横並び+FFT共有**)+全体をパステル配色(色は mainLnf 設定の1箇所に集約。v0.9.1でユーザー指定のミント基調=見出し/スライダー/つまみ/リセット矢印がミント、本文と数値が濃グレー、背景ほぼ白に確定。グラフ配色は入力ミント/出力ピンクのまま)。ユーザー保有のUI完成イメージ(サイドバー+カード式のかわいい系デザイン)への本格スキン化は未着手。**v0.10.0でMAIN/ANALYZEの2タブ構成**(TabbedComponent、ページはFnComponent。数値欄の白文字問題はsendLookAndFeelChange()で解決=スライダーのテキストボックスはLnF適用前に生成されるため必須)
- `dsp/VoiceAnalyzer.h` — **声質プロファイル分析(v0.10.0、依存ゼロ。推定器はv0.29.0で全面刷新)**。2048フレーム/hop1024、4xデシメYIN f0 →**真の包絡(反復ケプストラム・リフタリング、次数=fs/(1.6×f0)、8反復、+6dB/oct傾き補正)**→**候補ピークの同時割当**(順序制約+緩い範囲事前分布)でF1-F3。相対レベル・tilt・抑揚spread(p10-p90/2、MADは二峰性で破綻するため不可)。全て中央値でロバスト化。**v0.29.0追加**: 帯域別`rel[3]`(F/f0から算出する測定信頼度、2.5×f0未満=0/4×f0以上=1)と`vow[5]`(AEIOU別サブプロファイル、話者正規化した母音座標で分類、8フレーム以上で有効)。ANALYZEタブと offline_test で共用。**FFTは前方変換のみで両方向をまかなう**(対数スペクトルもケプストラムも実偶数列なので逆DFT=前方DFT/N。PsolaEngineへの追加不要)
- `src/PluginEditor.h` 内 AnalyzePanel — **ANALYZEタブ(v0.10.0)**: ①Record 5s(processorのcapBufに音声スレッドで録音)→Profile1 ②Load Target File(wav/mp3等、60秒まで、線形リサンプルでprevBufへ、Playで出力にミックス再生)→Profile2 ③Auto-Set=差分からpitch/formant/F1-3 shift&gain/range/center/tilt(0.25倍)を自動設定。v0.10.1: 手順をファイル→録音の順に変更、録音時間5/10/15sプルダウン(capTarget)、**個別フォルマントは控えめ設定**(shift=差×0.5・±3st、gain=差×0.5・±8dB。振り切ると乾いた電子音になるため)。分析側もF1探索下限=max(250, 1.35×f0)で基音の誤検出を防止(実例: F0 185Hzの声でF1=246と誤検出→F1 Shift+6に振り切れた)。**v0.11.0: Refineループ(手順④⑤)**=Record Converted + Refineで変換後出力を録音(capFromOutput=trueで最終段g*mをキャプチャ、ファイル再生音は混入しない)→目標との残差×減衰係数(pitch0.8/formant0.7/トリム0.4/tilt0.25/range比0.75-1.35クランプ)を現在値に加算。繰り返しで収束する設計。v0.11.1: ProfileGraph=測定プロファイルから逆算した想定スペクトル線グラフ(F0+F1-F3の山、高さ=相対レベル、F3以上-9dB/oct、50Hz-8kHz対数軸、塗りなし。You=ミント/Target=パステルイエロー/Converted=ピンク、測定完了時に静的更新)。v0.11.2: 見出し付き4セクション(Target/Analyze MyVoice/Re-Analyze MyVoice [Converted]/Apply Analyzed Settings、ユーザー指定の並び=Auto-Setは一番下)+「with Play」ボタン2種(ターゲット再生と録音を同時開始、録音完了で自動停止=playStartedByCapture)。v0.11.3: ユーザー評価「Pitchが強めに出る」→ kPitchBias=1st(F0差より1st控えめ)+kRangeBoost=1.15(Intonation増しで山の高さを合わせる。話し声の知覚的高さは山が担うため)。Auto-SetとRefine両方に同一バイアス(打ち消し合い防止)。Auto-Setは hifreq=自分のF0×2^((spread+2)/12)(通常域の上端、hipitch50%/hiformant100%も設定)と pitchfloor=目標F0×2^(-(spread+1)/12)(目標音域の下端)も自動設定。v0.11.4: ProfileGraphにIntonation横ヒゲ(F0±spread)と現在のhifreq/pitchfloorパラメータの縦破線マーカー(graph.paramでAPVTS参照)を表示。v0.11.5: with Playボタン2種→「With target play」チェックボックスに統合、Convertedプロファイルも行横に常時表示(pCLbl)、プロファイル名を「ターゲット(ファイル名)/MyVoice/Converted プロファイル」に統一
- `src/PluginEditor.h` 内 PresetPanel — **PRESETSタブ(v0.12.0/拡張v0.13.0)**: 全APVTS状態を `<userAppData>/VoxMorph/Presets/名前.vmpreset`(XML)に保存。v0.13.0: プルダウン=選択のみ(誤操作防止)→Loadボタンで適用。選択時に**プレビューグラフ**(標準声F0 140/F 500-1500-2500がそのプリセットでどう変わるかをProfileGraphで描画、入力非依存)。**PROFILES節**: proc.lastMyVoice/lastTargetを `.vmprofile`(profileToXml/FromXml)として保存。Delete(確認あり)、Reset All
- **ノイズゲート(v0.13.0)**: gateパラメータ(-80〜-20dB、-80=オフ)。processBlock先頭でエンベロープ追従(開1ms/閉25ms相当)ゲート、エンジン・分析・ビジュアライザー全ての手前
- **外部FXホスティング(v0.14.0/v0.15.0でチェーン化、スタンドアロン専用)**: JUCE_PLUGINHOST_VST3=1(**アンダースコア位置に注意、JUCE_PLUGIN_HOST_ではない**)。processor::addFx/removeFx/getNumFx/getFxSlot、FxSlot{plugin, atomic enabled}のOwnedArrayチェーン(pre/post)、リスト順に直列処理。音声スレッドはScopedTryLockで編集中1ブロックスキップ。モノ信号は複製→ステレオ処理→平均(applyFxMono)。UI=FxBar(スタンドアロン用オプションバー: Plugins.../Audio Settings...)→FxChainWindow/FxChainPanel(Pre/Post各リスト: On/Off・UI・X・+Add)。**アプリ本体ウィンドウはeditorのparentHierarchyChangedでネイティブタイトルバー化+旧Optionsボタン非表示**(Audio Settings...がバーに移設)。制限: FXレイテンシ補正なし。**v0.15.1: FX構成の永続化**=saveFxChains/loadFxChains(`<userAppData>/VoxMorph/fxchains.xml`、パス+enabled+プラグイン内部状態base64)。保存=チェーン編集時+デストラクタ(終了時、プラグインUIでいじった状態も捕捉)、復元=コンストラクタ末尾(standaloneのみ、消えたプラグインはスキップ)。UI方針: **共通機能=タブ、スタンドアロン専用=上部バー**。VST2はSteinbergライセンス終了のため対応不可(ユーザー了承済み)
- `src/PluginEditor.h` 内 AsmrPanel/SonarPad — **ASMRタブ(v0.13.0)**: asmrx/asmryパラメータ(-1..1)。円形パッドでドラッグ、上=正面/左右=方向/距離=減衰(最大-8dB)。定パワーパン(中央=完全ユニティ、モノ出力時は距離のみ)、出力段で適用(vizOut/Refine録音は影響なし=パン前のモノをタップ)。ダブルクリックで中央復帰
- `test/offline_test.cpp` + `analyze.py` — 合成母音での数値検証(Linux g++でコンパイル可、JUCE不要)。**エンジン変更時は必ず実行**: f0/フォルマント独立性、抑揚、子音シフト、フライ声、VoiceAnalyzer、回帰一式

## 主要パラメータ(内部ID)

pitch, formant, consonant, f1shift/f1gain/f2shift/f2gain/f3shift/f3gain, vadapt/vamount/vcharacter(AEIOU Character, 既定オフ/60%/Natural)+Custom15値(va_{a,i,u,e,o}_f{1,2,3}), range(抑揚)/center, breath2(Beta), air(Natural Air, 0〜1.5・標準経路常時使用), airshine(Air Shine, >6kHz抜け感0〜6dB), 【deprecated no-op: airband/air2/air2low=旧セッション互換のため登録のみ】, gci(GCI同期), hifreq/hipitch/hiformant(High Rangeガード), tilt, jitter, robot/robotHz, lowvoice, lowlat(Legacy Low Latency、BETAウィンドウ), perfmode(Performance Mode), pitchfloor, automute, gate(ノイズゲート), asmrx/asmry(ASMR位置), mix, gain

## 重要な設計判断・経緯

- Vocal Transformerはgranular(PSOLA近縁)方式 — Logic 9マニュアルに明記。同系統を自作した
- 二重声バグ→グレイン幅を出力間隔に適応(v0.3.2)。低音フライ声→Low Voice Mode(40Hz+ホールド、v0.3系)
- Breathは加算方式→HNM置換方式まで試したが**ユーザー評価は「電子ノイズ」でBeta棚上げ中**。次の本命=連続ノイズを声道フィルタに通す方式(グレイン毎ノイズ再生成をやめる)
- 大バッファのブツブツ/無音バグ(v0.5.2で解決)=自動ミュートのバッファ依存誤爆+検出頻度低下
- Air Preserve(v0.6.0)=息混じり母音の金属的ジャリジャリ対策。グレイン再配置がノイズに新ピッチの周期性を刻む(上方シフトのグレイン再利用で最悪)のが原因で、息成分をシフト前に分離しバイパスする。コムの補間は線形だと高域キャンセルが甘く旧ピッチのゴーストが漏れるため**Catmull-Rom必須**
- v0.6.1: ユーザー評価「きれいになるが変化が微小」→ 帯域を1.4k→1k既定+airbandノブ化(下げるほど強い)、ノブ0.7超で息ブースト追加。数値検証: HF周期性 素材≈0 / off=0.34 / on0.8=0.06 / max(1.0,帯域700)=0.04、f0・フォルマント保存
- 推奨バッファ256@48k。バッファは音質に影響しない設計
- バージョン番号は `CMakeLists.txt` の project(VERSION) と `.github/workflows/build.yml` の pkgbuild --version の**2箇所**を同時に上げる
- **Natural Air v2(v0.20.0、air2トグル既定オフ、Beta)**: Air Preserve内部アルゴリズムの世代交代試作(計画書=`AIR_REDESIGN_PLAN.md`)。同じ因果ピッチコム残差を**相補4帯域**(one-pole LP減算連鎖、~700/2500/6000Hzクロス、b0+b1+b2+b3≡入力で再構成誤差ゼロ)に分割し、帯域毎のkeepゲインを制御レート(512サンプル毎、detectPitch直後)の**残差×ピッチラグ正規化自己相関**で駆動: 残差がピッチ周期と相関=倍音漏れ(旧ピッチゴースト)→遮断、無相関(純ノイズはコム特性でρ≈-1/6)→全量バイパス。ラグは±2%探索(平滑curPの追従遅れ補償)、**F0モーションゲート**(knee 0.8%/フレーム+0.25%超で6フレームホールド、増加はYIN明瞭度×静止度でゲート・減少は常時通過=動いても保守側にしか動かない)、帯域別最小統計ノイズ床で背景ノイズのair化を抑制(保持量の補助制御のみ、信号からの減算はしない)。airノブ/airbandノブは共通(airbandは帯域重みバイアスに再解釈)。数値検証: ビブラート時旧ピッチゴースト -22.8→-27.6dB、breathy定常HF周期性 0.085(legacy 0.084と同等)、純倍音入力でrel diff 0.0000(完全無害)、レベル中立±0.02dB、追従遅れ追加なし、off時はlegacyとbit-identical。CPUはオフライン代理計測でlegacy比+0.55%(0.89→1.44%)。**ホスト報告レイテンシ増なし(D不変)**。実機AU/VST3/Standalone・複数バッファでのCPU実測とユーザー実声評価は未了
- v0.20.0でRTスレッド初回alloc対策: `dbuf.reserve` を prepare に追加。offline_testにアロケーションカウンタ・NaN/Inf・bit-identicalバイパス・帯域再合成・既知ノイズ/ビブラート/グライド/歯擦音/無声→有声遷移の検証群を追加(製品表記に「WORLD」「D4C」は使わない方針)
- **v0.24.0: Natural Air 標準化(安定版)**。Natural Air改善フェーズ完了に伴い、v2帯域適応コム+FFTクリーンアップ+低F0ノッチバンクを**常時使用の標準経路**に統合し、旧Air Preserve経路(単一HP残差)をエンジンから削除。airband(帯域ウェイトバイアス)は旧既定1000Hz時の値 **{0,1,1,1} を内部固定定数kWBand**に凍結(スライダー撤去で音は不変)。**APVTS互換**: `airband`/`air2`/`air2low` は旧セッション/プリセット/オートメーション互換のため**非表示no-opパラメータとして登録維持**(DSPは読まない、deprecatedコメント付き。ホスト互換確認まで物理削除しない)。公開パラメータは `air`(表示名Natural Air)と `airshine`(Air Shine)のみ。UIはVOICE QUALITYにNatural Air+Air Shineの2行だけ(Beta表記・方式切替の記述は撤去)、Low Voice ModeはADVANCEDの独立オプションのまま(ツールチップを機能目的+トレードオフ明記に更新、「Natural Air Low」という名称はUIに存在しない)。実験フック(grainBlend/grainAvg/grainHalfP/PSOLA_GRAIN_LOG)は既定オフのまま温存。テストは「air OFF vs ON」基準に再構成し全PASS(bit-identicalバイパス、Shine 0/3/6特性、>6kHz不変、alloc 0、レイテンシ不変、CPUオフライン代理 off 0.92%/air on 1.93%)。低音ゴーストは実用範囲まで改善済みであり、これ以上の強い抑制は音色保護のため行わない(方針凍結)
- **v0.23.0: Natural Air 低F0クリーンアップ(air2lowトグル、既定オン)**。ユーザー5条件実録(GhostTest 109Hz +12st)で「Air ONで入力F0奇数次+0.9〜1.2dB」を確認(当方のピーク指標でも+1.0〜2.7dB再現。以前の「Air OFFでも不変」は広帯域積分指標の欠陥)。漏れは2.5-6kでなく**0.1-2.5k(b1系)**に集中。第2コム方式は第1コムと同じ周期誤差で線を取り逃すため不採用とし、**適応ノッチバンク**(奇数k×f0、100-2500Hz、最大12個、BW=max(25Hz,3%fc)、f0追従バイクアッド)をb1+b2エアに適用。ゲート=pitchConf×平滑F0変動(持続母音のみ、グライド/立ち上がり/無声で解放)。実測: Air ON奇数次増分 LV OFF +2.4→+0.8dB / LV ON +1.0→+0.4dB、偶数次・2-6k・6-16k(Air Shine)不変。air2lowオフ=v0.22.0動作(A/B用)。CPU+0.09%(低F0アーム時)。grainBlendは実聴差なしのため既定化保留(フック温存)。詳細=AIR_REDESIGN_PLAN.md Phase 2.1
- **v0.22.1: グレイン経路A/B実験フック+実験レポート**。低音+12stゴーストの系統実験(詳細=`GRAIN_AB_REPORT.md`)。結論: ①グレイン幅は主因でない(現行適応則がほぼ最適、0.5Pでも+1.5dB/body-13%) ②GCIは無関係(ONでむしろ悪化) ③再利用パターンの乱れとghostが強相関(-0.84) ④**可聴ゴーストの主因は有声/無声判定**: 入力の周期倍加区間(53Hz<通常モード下限60Hz)と減衰テールが無声判定→無声経路はピッチ変換しないため旧ピッチ素通し。**Low Voice Mode ONで解消方向**(+blend/avgで総合-26.9dB)。実験フック(Params: grainHalfP/grainBlend/grainAvg、全て既定オフ=bit-identical、UI非公開)と PSOLA_GRAIN_LOG(コンパイル時計測、プラグイン非影響)を追加。grainBlend=分数位置パルスクロスフェード(全シフトで艶↑・AM半減・middle ghost -48→-51dB)は実声試聴後に既定化を検討
- **v0.22.0: Natural Air v2 スペクトルクリーンアップ(Phase 2先行)**。ユーザー実録(KITUNE、44.1kHz、+12st)で切り分け: middleレンジはair分離段階のF0周期漏れ(air完全オフ比+4.8dB)、**lowレンジのゴースト(-21.9dB)はair完全オフでも同値=グレイン再合成経路由来(+12stではグレイン幅が入力1周期を超え、奇数次入力倍音が出力倍音の中間に残る)で本件では未解決・別課題**。対策(ユーザー指示の方式2を採用): noiseBufは出力までDサンプル遅延の余裕があるため、1024点FFT/hop512/周期Hann OLA(ユニティ再構成)を**追加レイテンシゼロ**で挿入し(processAirFx)、有声時に入力F0整数倍近傍(6kHz以下のみ、幅±max(1, 2%C)を間隔の35%でキャップ)のビンを倍音間フロア(隣接中点magの小さい方×1.4)へクランプ。>6kHzは不変=**Air Shine完全維持**。f0<~130Hz(倍音間隔<3bin)はクランプ無効(低域はlowCons/クレストガードが担当)。Low Latencyモード(D<窓長)は従来どおり生バイパス+airFxHopは追従のみ。方式1(定常母音でBand0-2に上限)はairband=4000代理で比較→ゴースト低減は同等(-48.0 vs -47.8)だが中低域エアを構造的に失うため**不採用**。実測(KITUNE_middle入力をエンジン通し、+12st): air経路ghost -43.2→**-47.8dB**(air0=-48.0と一致)、6-16k・Shine特性・レベル不変。offline_testに+12st持続母音ゴースト回帰(ghostDb、fftForViz流用)追加。CPUオフライン代理: v2計1.90%(legacy 0.94%、クリーンアップ分+0.12%)。※ユーザー音声ファイルはローカル解析のみ、リポジトリ非収載
- **v0.21.0: 低音ゴースト修正 + Air Shine**。ユーザー実声評価(v0.20.0): ウィスパー良好・ビブラート/グライドの中域ゴースト大幅改善、ただし**低い「あー」持続で旧ピッチゴースト**、高域の抜けはさらに強化したい。①原因=period doubling/交互周期/軽いフライの倍音漏れは**2Pで相関しPでは弱い**ため非周期と誤認(offline再現: サブハーモニック-15%でb1のkeepが0.99まで上昇)。対策: 漏れ判定の候補ラグを{P, 2P, P/2}に拡張(各±2%、ストライド2+±1精密化、分母エネルギーはprefix和でO(1))。②さらに**クレストファクタガード**: ランダム周期ジッタのパルス残差は「どのラグでも無相関=技術的には非周期」だが旧ピッチに聴こえる。息ノイズ(ガウス的、crest~3.5)に対しパルス残差はcrest≫6で明確分離→crest4.5-7でkeepをフェードアウト(90Hz定常のrel diff 0.042→0.006)。③低F0(<130Hz)ではb0/b1のkeep上昇レートを減速(lowCons)。検証: サブハーモニックb1 keep 0.99→0.14、全既存回帰PASS維持。**Air Shine(airshineパラメータ、0〜6dB、既定0)**: v2専用、最上段バンド(>6kHz)の**noiseBuf再加算ゲインのみ**を持ち上げる(harmBuf側の減算・中低域は不変)。breathy実測: +6dBで6-16k帯域比0.0176→0.0291、1-4k・RMSはほぼ不変。CPUオフライン代理計測: legacy 0.97%→v2 1.78%(+0.8%/コア)

- **v0.41.2: キャラクター選択の紺色バンドをウィンドウ左右端まで到達させた**。
  - **原因はユーザーの推測どおり**: MATCHINGページは他のページと同じく `pageArea`(= `r.reduced(12, 8)`)に置かれていて、**左右12pxの余白が残っていた**。**コンポーネントは自分の bounds の外に描けない**ので、パネルの中でどう頑張っても端には届かない
  - **対策**: MATCHINGだけ **ウィンドウ全幅**(`pageArea.withX(0).withWidth(getWidth())`)で bounds を取り、**ページ側のインセットをパネル内部で付け直す**(`MatchingPanel::kEdge = 28` = ページ12 + コンテンツ16)。これで文字の位置は従来どおり他タブと揃ったまま、帯だけが端まで伸びる。**kEdgeがタブ間の列揃えを保っている唯一の値**なので、動かすとページ間で揃わなくなる
  - **検証はピクセルで行う**(`ui_shot`)。帯はコンポーネントではなく描画なので bounds を問い合わせられないし、**そもそも bounds は証拠にならない** — コンポーネントは自分の外に描けず単にクリップされるだけなので、「本当に端まで描かれたか」はレンダリング結果の画素を見るしかない。カード行の中心Yで左端(x=0)と右端(x=W-1)の画素を採り、**明度<0.35**(バンド 0xff262b45 は約0.17、ページ地 0xfff3f5fb は約0.96)を確認。実測 `191B22` / `191B22`
  - DSP無変更、offline_test・bitexact ともに ALL PASS

- **v0.41.1: マッチングタブのレイアウト微調整(デザイン画への追い込み)**。指定5件、実ビルドの描画で確認(最小1180x920/既定1400x1080ともはみ出し0)。
  - **①説明文をヒーローサークルの張り出しの横へ**。従来 MATCHING は他のサイドページと同じく **バルジの下から**開始していたため、**ウィンドウ幅いっぱいの死んだ帯**ができていた。MATCHINGだけ MAIN と同じ `pageArea` 全体を受け取り、**円の張り出しを避けて自分で組む**third caseに。`MatchingPanel::setBulge(中心, 半径, 下端)`
    - **バウンディングボックスではなく円として扱う**。張り出しは円なので、中心(バンド内、このページの外)の高さで最も広く、下るほど狭まる。矩形で扱うと**実際に使える幅の大部分を捨てる**ことになり、最小幅で説明文が「システムで/す。」と折り返していた。`bulgeHalfWidthAt(y)` で該当行の実幅を出す
  - **②キャラクター画像をカード全面に**(角丸クリップ + `fillDestination`)。円形だとカードの大半が無駄になり、全員が「遠くの小さな顔」になっていた。**非選択は白のベールを重ねる**(hover 0.34 / 通常 0.52)。**彩度を落とすのではなくベール**なのは、元絵がほぼモノクロなので彩度を触っても何も起きないため。文字はアートの上に載るので白い下敷きを敷いた
  - **③紺色背景をヒーローバンドと共通化**。`ak::paintBand` を**そのまま呼ぶ**(近似ではない)ので、格子タイル・奥行きグラデーション・内側シャドウが完全に一致し、**今後ズレようがない**。ウィンドウ両端まで拡張(白い余白が出ると不具合に見えるため)。上下の余白は 24px → **12px** に圧縮
  - **④MY VOICE の上に余白**(`jlimit(18,54, 高さ/12)`)。TARGET→MY VOICE と MY VOICE→AUTO MATCHING の間隔を揃えるため
  - **⑤DNAをウィンドウ水平中央へ**。**らせん本体**が中央線に乗るよう配置している(コンポーネント自体はブラケットが右に張り出すぶん非対称なので、コンポーネントを中央に置くとらせんが左にずれる)。`x = 中央 - 0.32×幅`
  - **用語**(ユーザーからの質問「あの部分は何と呼ぶ?」): コード上では **ヒーローバンド(hero band)** = ヘッダー下のタブと円を含む濃紺の帯(`bandArea` / `ak::bandPath` / `ak::paintBand` / `ak::bandTop`,`bandBot`,`bandInk`)。中央の円は **ヒーローサークル / キャラクターバッジ**(`HeroCircle`)、円の周りで帯が膨らんでいる部分が **カラー(襟, `ak::kHeroRim`)**、その下端が `bulgeBottom`
  - DSP無変更、offline_test・bitexact ともに ALL PASS

- **v0.41.0: マッチングタブのレイアウト刷新(ユーザー提供のデザイン画に合わせる)**。指定6件。**全て実ビルドのオフスクリーン描画で確認**(最小1180x920と既定1400x1080、はみ出し0)。
  - **①本文左上に説明文**。パステルのグラデーションカードに2行。「このタブは何か」という唯一の疑問に最初に答えるものなので、コントロールではなく注記として読めるよう配色した
  - **②ターゲットキャラクターをポートレートカードに**(`CharacterCard`)。**アートが本質**: 数値が半音以下しか違わない7体を「Uru」「Kura」という文字で見分けられるはずがなく、従来の同一のwifi風アイコン列は誰なのか何も語っていなかった。**センター揃えの横並び**、背景は**ダークな帯**(`stripArea`、ヒーローバンドと同じ色)。**淡いライン画を明るいページ地に置くと輪郭が消える**ので反転させた。カードは`ak::line`枠+選択時は`ak::sidebarSel`の太枠(暗い帯の上でのグローは隣のカードに滲むため枠で表現)
    - 画像は `assets/char_{uru,kura,sara,soshi}.png`(1254px原寸を**sipsで256pxへ縮小**、計288KB。タイル表示は最大150px程度なのでこれで十分)。`SampleTargetEntry` に **`image` フィールド**を追加、**空文字は「アート未着」で正常状態** — カードはプレースホルダーの丸を描く。ふに/まき/ゆには画像待ち
    - **【重要】`assets/*.png` は `file(GLOB)` で configure 時に確定する**。PNGを足したら **`cmake -B build` を再実行しないとBinaryDataに入らない**(最初これで全部プレースホルダーになった)
  - **③RECORD / MyVoiceFile を MATCH と同じサイズ・同じ形に**(`PillButton`、幅170)。押す物が3つあるのに「四角いタイル2つと横長ボタン1つ」だと別種のコントロールに見えるため。RECORDの先頭の点は`paintButton`で描く(録音中に色を変えてもラベルを組み直さなくて済む)
  - **④SAVE PRESET削除**。ヘッダのプリセットプルダウンが全タブで保存を提供しているので、同じ部屋への2つ目のドア。**NEW CHARACTERは残す**(保存する物が別種)。付随して**インラインの名前入力欄一式も削除**
  - **⑤プロファイル数値の表記(`Target: さら F0 318Hz...`)を削除**。推定器がたまたま保持している数字を、**声を選ぼうとしている人**に見せていた。選択はカード自身が示し、伝えるべきことはステータス行へ回す。**`fmt()` も道連れで削除**(他に使い手がいない)
  - **⑥中央にDNAと関係線**(`DnaFlow`)。**アートではなく描画** — ウィンドウに追従し、パレットから外れないため。見出しの右に伸びる罫線 → 二重らせん → 右側のブラケット → 次の見出し、で**1本の線が図で中断されている**形にした。罫線もブラケットも**MAINタブのグループ括弧と同じ1px `ak::treeLine`** なので、両ページが同じ製品に見える。ラング(横棒)は**交差点付近でフェードする** — 交差点では2本の鎖が同一点になり、そこに横棒を引くと「インクの染み」=不具合に見えるため
  - **UIハーネス側の更新**: SAVE PRESETは「無いこと」を、RECORDは「有ること」を検査。加えて **`Target:`/`MyVoice:` で始まるLabelが0件であること**を検査(表記削除の回帰)
  - **ハーネスが見つけた実バグ1件**: `CharacterCard` が `setButtonText()` を呼ばず、キャプションを private メンバに持っていたため**アクセシビリティにも監査にも名前が見えず、タイル数0と報告された**。Buttonのテキストは表示だけの持ち物ではないので、キャプションを Button 自身に持たせて解消
  - オフラインテスト・bitexactも ALL PASS(DSPは無変更)

- **v0.40.0: Air/Air Shineを.vmprofileに保存 + Airの算出根拠をターゲット側へ変更**。ユーザー指定2件。
  - **①`profileToXml`/`profileFromXml` が `hnr[3]` / `hfDb` / `tractScale` を保存・復元するようにした**。測定はずっとしていたのに書いていなかったため、**すべての.vmprofileがテクスチャを失っていた**。Auto-SetのAir/Shine段は「ターゲットがテクスチャを持っているか」で自己ゲートするので、**保存プロファイルは一度もAirを設定できていなかった**。NEW CHARACTERは特に「声を記述するもの」なので、息の情報が無いのは記述として不完全
    - **無い属性は0のまま**にする(捏造しない)。**0dBは「純ノイズ」という実在の値**なので、もっともらしい既定値を入れると「最大限に息っぽい」と読まれる。v0.40.0以前のプロファイルは従来どおりAirに触らない
  - **②Airの算出をターゲット自身の息っぽさから取るように変更**。**旧式は `air ~ (ソースのHNR − ターゲットのHNR)`** で、「ターゲットはソースより息っぽいか」を訊いていた。しかし**バンドHNRは声の息っぽさと録音のノイズフロア/マイク/コーデックを混ぜた量**であり、**「自分のマイク」対「キャラクターのサンプル」は条件が比較可能になりようがない組み合わせ**。少しノイジーなマイクで録ると全ターゲットより息っぽいと判定され、**実際には部屋の状態を報告しているだけの式からair=0が返る**
    - **Airの実際の役割は「ソースに在る息を、ピッチ変換で潰さずに通す」こと**。どれだけ通したいかは**結果がどれだけ息っぽくあるべきか＝ターゲット単独の性質**で決まる。よってターゲットの上位バンドHNRを下限に使い、差分項は「それ以上に必要なら」上乗せする形に
    - **スケールの根拠**: 純ノイズは構造上0dB、通常の発話は+7〜19dB(VoiceAnalyzerの既存計測)。内蔵7体はバンド2-3平均で3.2〜9.4dBに入るので、ランプを 12dB(clean)〜3dB(breathy) とした。結果 **Funi 0.19(最も息が少ない)〜Maki/Yuni 0.59(最も多い)**、**ソースの録音品質に依存しない**
    - **これは「その録音がどれだけ息っぽく聞こえるか」の推定であって、声の純粋な性質ではない**(ノイジーなサンプルは息っぽいと読まれる)。それでも0を返すよりましと判断した — **Airが0なのはこれらの声では明確に間違いで、「意見なし」は既定オフのパラメータにとって中立な答えではない**ため。ユーザーが推定値/固定値を許容した箇所
    - **Air Shineは差分のまま**。`hfDb` は1つの録音の中での比(6-16kHz対音声帯域)なので、レベルやゲインの違いに強く、2つを比べるのは公平。ソース側にテクスチャが無ければ従来どおり触らない
  - **`predictEstimated` のテクスチャ**: `hnr` は**継承**(Natural Airはソースの息を保存する方式で新しいノイズを合成しないため、変換後のHNRは実質ソースのもの。ここで動かすのは測定を装った当て推量になる)。`hfDb` は**調整する** — Air Shineは>6kHzのゲインそのもので、効果も実測済み(+6dBで帯域比+2.2dB = **Shine 1dBあたり約0.37dB**)。入れないと、Shineをかけて保存したキャラが実際より暗く記録され、後でそれに合わせる時に同じ分だけShineを過小要求する
  - **検証**: offline_testに2件 — **(g3)ターゲットより明らかにノイジーなソースでもAirが出る**(7体で0.17〜0.59、これが壊れていたケース) **(g4)テクスチャ未測定のターゲットには一切Airを書かない**。ui_shotに**.vmprofileラウンドトリップ**(hnr/hfDb/tractScale/母音テーブルの保存復元、**再読込したプロファイルがAirを駆動すること**=NEW CHARACTERに必要な性質、**v0.40.0以前形式が0のまま読まれて無音を保つこと**)。`profileToXml`はJUCE依存でoffline_testから触れないため、**ui_shotが唯一のテスト場所**
  - 全テスト・bitexact・UIハーネス ALL PASS

- **v0.39.0: マッチングタブのUI整理 + NEW CHARACTER + 既製5種の削除 + UI検証ハーネス**。ユーザー指定の5件。**今回から実ビルドでのオフスクリーン検証を実施**(下記ハーネス)。
  - **①既製5種(自然な女性声〜中性的な声)を削除**。カタログは実測7体のみに。**理想値エントリは構造的に品質が低い**: 手書きの統計量で `rel={1,1,1}` 固定、母音テーブルが無いのでグローバル・フォールバック経路しか通れず、AEIOUマップを一切書けなかった。**副産物としてタイル行の逼迫が解消**(13個→8個、96pxで行幅824px/利用可能1124px)。なお `MatchingEngine` のフォールバック経路は**死にコードではない** — v0.29.0より前の.vmprofileや、母音バケツが埋まらない短い録音が今も通る(offline_testで直接カバー)
  - **②TargetFileタイルがメニューを開く**ように変更(読み込み / 再生・停止 / プロファイル保存)。TARGET CHARACTERセクションの **Play・Save Profileボタンは削除**
  - **③MyVoiceFileタイルも同様にメニュー化**。MY VOICEセクションの **Save Profileボタンは削除**。**再生のために `myBuf`/`myLen`/`myPos` をProcessorに追加**(prevBufの流用は不可 — MyVoice読込でTargetプレビューを壊さない約束[spec 5.6]があり、録音中の「With target play」で両方が同時に armed になりうるため)。processBlockはprevBufと同形で、`myPos==-1`(既定)なら完全に何もしない。**bitexact通過**。**.vmprofileのMyVoiceとRecordした音声には再生対象が無い**ので、メニューの再生は音声ファイルを読み込んだ時だけ有効
  - **④Reset All to Defaults をマッチングタブから削除し、ヘッダのプリセット選択プルダウンへ移動**。全パラメータを戻す操作がマッチングタブ限定なのは置き場所として不自然だったため。**選択リストの中の「アクション」なので、選んだ後にプルダウンの選択を元へ戻す**(戻さないと、そのプリセットが選択中であるかのように見える)。id=90001はプリセットindexと衝突しない値。**PRESETSタブ側の同名ボタンは残置**(削除指示は無く、あちらは元からある)
  - **⑤NEW CHARACTERボタン**をSAVE PRESETの隣に追加。**MyVoiceがある時のみ有効**。**今の設定がMyVoiceから作り出す「声そのもの」を.vmprofileとして保存**する(設定ではない)。保存後はTargetとして選べる。**.vmpresetでない理由**: プリセットはつまみの位置の集合で、ここで欲しいのは結果の「声」= 別種のオブジェクトであり、Target側が食うのはこちら
    - **`MatchingEngine::predictEstimated` が母音テーブルも変換するようにした**。従来はグラフがグローバル値しか読まないので `vow[]` を放置していたが、**NEW CHARACTERはこれをディスクに書く** — グローバルFだけ動いて `vow[].F` が動いていないプロファイルは内部矛盾しており、Targetとして読み直すと母音マッチ経路(vow[]を読む)が未変換の値と比較してしまう。AEIOUマップ(va_*、vamount/vadaptでスケール・ゲート)も含めて適用
    - **これは予測であって、変換結果の録音ではない**。実測が欲しければ Record + capFromOutput(Re-Analyze)が既にあり、あちらは実際に喋る必要がある。NEW CHARACTERは喋らずに済ませるためのもの
    - **既知の制限**: `profileToXml` は `hnr`/`hfDb` を保存しない(v0.29.0からの仕様)。よってNEW CHARACTERで作ったプロファイルをTargetにしても **Air / Air Shine は自動設定されない**(`tgtMeasured==false` の分岐に落ちる)。全.vmprofileに共通の既存挙動
  - **【新規】`test/ui_shot.cpp` — オフスクリーンUI検証ハーネス**(`-DVOXMORPH_UI_HARNESS=ON` で `VoxMorphUiShot` をビルド)。**v0.36.10の教訓を仕組みにした**: `ed->addToDesktop()` で実ピアを持たせ、`isShowing/uiWantsViz/uiWantsMeters` を毎回印字する(0なら、それらが駆動する物についてスクショは証拠にならない)。加えて**目視でなく数値で監査**する — ①親からはみ出した子(Viewportのスクロール内容は正当なので除外)②あるべき/あってはならないボタンの有無 ③タイル数とサイズ ④**Reset Allは挙動で検証**(パラメータを既定から動かす→項目を選ぶ→戻ったか＋プルダウンの選択が復帰したか)。最小1180x920と既定1400x1080で **ALL PASS**、PNGも出力
    - 検証中にハーネス自身の欠陥を2つ見つけて直した: **(a)** ボタン検索がエディタ全体を舐めていてPRESETSタブのReset Allを誤検出 → **Matchingページに限定** **(b)** Viewportのスクロール内容を「はみ出し」と誤判定 → 除外規則を追加。**検査自身が間違っていることがあるので、失敗したらまず検査を疑う**
  - `.gitignore` に `build/` を追加(JUCEビルドツリー)

- **v0.38.0: ターゲットキャラクターを3体追加(ふに/まき/ゆに)、カタログ計12件**。v0.37.2の推定器で測定。生成器の出力とバイト一致を確認済み(7体全部)。
  - 実測値: **ふに** f0 342.1Hz / spread 4.50 / F 893-2041-3686 / tilt 13.16 / 1135フレーム、**まき** f0 303.4 / 3.52 / 948-2224-3674 / 21.47 / 2553、**ゆに** f0 312.2 / 2.80 / 851-2028-3753 / 23.26 / 2318。3体とも5母音すべてが有効フレーム数を満たす
  - **rel[0]がf0順にほぼ単調に並んだ**: そし(252Hz) 0.45 → まき(303) 0.21 → ゆに(312) 0.14 → さら(318) 0.02 → ふに(342) 0.00。**`density()` が符号化している法則そのものが、一度もフィッティングしていない7つの独立した録音から出てきた** — 推定器の妥当性の傍証として記録しておく
  - 男声合成ソースからの formant は +3.93 / +3.75 / +4.03 st で、既存4体(+4.28〜+6.98)と整合。全12件でテスト通過
  - **【要注意】ターゲットタイルの行がほぼ満杯**。タイルは幅650px以上なら**全数を1行**に並べる実装(`MatchingPanel::resized`)で、13個(12件+TargetFile)だと**最小ウィンドウ幅1180pxで タイル79px・行幅1123px / 利用可能1124px = 余白1px**。今回は収まるが、**次に追加するときは先にレイアウトを直すこと**。
    - **単純な折り返しは不可**: 最小ウィンドウ高さ920pxだと1行でもグラフ+ステータスに116pxしか残らず、2行にすると**12px**になって比較グラフが潰れる(算出済み)
    - 現実的な解は**タイル行を横スクロールのViewportに入れる**(タイル96px・1行を維持でき、縦の予算に影響しない)。これはJUCEの実ビルドでの目視確認が要る変更なので、今回は着手していない
  - 補足: 依頼文は「ふに、ゆき、まき」だったが、共有されたファイルは **ふに / まき / ゆに**(ゆ**に**)。Downloadsに「ゆき」は存在しないため、添付どおり **ゆに** として追加した

- **v0.37.2: 【重大バグ修正】3つのバンドが「同じピーク」を選んでいた + 母音分類をF3正規化へ**。ユーザー実声評価「**Saraモデルだけうまくいかない(Formantがマイナスになる)**」の調査から。2つの修正は**セットで初めて効く**(下記)。
  - **原因1: 探索レンジが重なっているのに、各バンドが独立に最大値を取っていた**。F1の探索範囲 700-1200Hz は F2 の 700-3400Hz の内側にある。高い声でF1とF2が1つの包絡の山に融合すると、**両方のバンドが同じピークを選ぶ**。実測: 実キャラ4体で **F1とF2が同一ピーク = 30〜46%のフレーム**、F2とF3 = 20〜35%。その後ろにあった最小間隔の行(`Fout[1] = max(Fout[1], Fout[0]+150)`)が重複を150Hz引き離していたが、**それは2つの測定値ではなく、1つの測定値とその算術的帰結**であり、`obs` はどちらもtrueだった
  - **Saraが壊れていた理由**: 4体で最も高い声(f0中央値318Hz、p90 445Hz)なので重複が最悪の**45.5%**。結果 F2 が 1284Hz(他は1477〜1643)と読まれ、**男声からのformantが +0.48st**(他は+4.19/+4.98)= 「あなたの声道はもう適正な長さです」と言っていた。実声でマイナスになったのはこのため
  - **修正1(dsp/VoiceAnalyzer.h assignFormants)**: **3バンドは必ず異なるピークを取る**。ピークは周波数順に並んでいるので、インデックスが厳密に増加することを要求すれば **F1<F2<F3 も自動的に保証される** — 順序が「後から直すもの」ではなく「選び方の性質」になった。前のバンドより上にピークが残っていないバンドは**単に未観測**(トラッカーの保持値は使うが統計には入らない)。最小間隔の修復行は**削除**
  - **原因2: 母音分類がその録音自身の「F2の中央値」で正規化していた**。何を喋ったかで座標が動く — /o/ が多い素材だと中央値が下がり、全フレームが前寄りにずれる
  - **修正2(classifyVowels)**: **話者自身のF3で正規化**。F3は母音空間での変動が約1.7st(F1/F2は5.9st、`MatchingEngine::kVtlW` と同じ根拠)なので、**log(F2/F3) はほぼ純粋な母音アイデンティティ**になり、録音全体の統計を必要としない。アンカーは公表値(Yazawa & Kondo)の男女平均から作った log2(F1/F3), log2(F2/F3) の固定比。**旧 `vowelCoord()` と kAnchorH/kAnchorF/kAnchorF2 は削除**(VowelAdaptiveWarpは独自のコピーを持っているので無関係)
  - **【重要】2つは片方だけでは効かない**。母音バケツの正しさ(バケツのF2が、そのラベルの母音の真のF2からどれだけ離れているか。中央値/p90、単位st):

    | 構成 | バケツF2誤差 |
    |---|---|
    | v0.37.1(両方旧) | 1.32 / 7.84 |
    | ピーク重複修正のみ | 1.45 / 8.04 |
    | F3正規化分類のみ | 1.57 / 6.83 |
    | **v0.37.2(両方)** | **0.94 / 3.78** |

    片方だけだと**どちらも悪化して見える**。ピーク割り当てを直すとF2の分布が動き、旧分類器はその分布の中央値で正規化していた=**直そうとしている誤差を部分的に打ち消していた**から。両方の結合を切って初めて改善する
  - **効果(ground truth)**: フォルマント精度 中央値 0.67 → **0.43st**。母音検出 3/5 → **4/5母音、worst F2誤差 5.21 → 2.70st**。男声→女性/アニメ声の復元 worst 2.50 → **0.80st**(マイナス0件)。フレーム単位の母音分類精度は 56.9% → **63.5%**(ラボ計測)
  - **カタログは3度目の測り直し**。F2 1284〜1643 → **1972〜2033**、F3 2658〜3251 → **3586〜3876**。**4体のF2/F3がほぼ揃った(F2で約0.3st差)のは偶然ではなく健全性チェックが通ったということ** — 同系統・同サイズの4声なので本来揃うべきで、旧割り当てではバラバラだった。Sara は **+0.48 → +6.81st** で他と横並びに
  - **検証(offline_test 追加2件)**: ①**フォルマントが重複せず順序を保つ**(旧修復の署名 = F2−F1 がちょうど150Hz、を検出) ②**母音バケツが自分の母音を保持しているか** — 「フレームが均等に分かれているか」では**均等に間違っていても高得点になる**ので不採用にし、既知フォルマントの5母音発話を分析して**各バケツのF2が、そのラベルの母音の真のF2からどれだけずれるか**で採点(F2は母音空間で約16st動くので、偶然当たることがない)
  - **既存テストの前提を1つ修正**: 「カタログ全エントリで formant が正」という判定は、**`androgynous`(中性的な声、f0 175Hz)には成り立たない前提**だった。中性的な声道が特定の男性より必ず短いとは限らず、正直な答えはゼロ近傍でどちらにも振れる。**f0が話者の1.25倍を超えるターゲットにのみ正を要求**し、それ以外は「大きく振れないこと(|formant|<3st)」を要求する形に変更
  - **残課題**: 分類の誤りは主に **/e/ と /i/**。この2つはアンカーが log(F1/F3) で -2.561 対 -3.319 と大きく違うのに log(F2/F3) では -0.403 対 -0.387 とほぼ同じ = **F1でしか区別できない**。F1が測れない高さの声では原理的に分離できない。**エンジンの音は不変**(bitexact通過)

- **v0.37.1: 【重大バグ修正】F1が「原理的に測れない」のではなく「測れていなかった」**。v0.37.0で4体とも `rel[0]=0.00`・F1が話者のf0より低い、という状態だったのを調査した結果、**物理的な識別限界ではなく実装バグ**と判明。カタログは修正後の推定器で**全部測り直した**。
  - **原因**: `harmonicEnvelope` は第1倍音より下を **H1のレベルのまま平坦に外挿**する(`if (k <= pkB[0]) lv = pkV[0]`)。そこには測定値が無い。ところが `extractPeaks` の極大判定が `V[k] >= V[k-1] && V[k] >= V[k+1]` で、**平坦な区間は全点がこれを満たす**。結果その外挿区間が「H1と同じ高さのピーク群」として湧き、`assignFormants` はレベル最大で選ぶので、F1帯に本物の極大が無いとき**この偽ピークが勝つ**。合成/i/ f0=350(真F1=352)で **141〜275Hzに13個、全部46.1dB**(平坦の署名)、F1帯に本物の極大はゼロ。**不確かな推定値ではなく、無情報区間の左端を確定値として返していた**
  - **副次的な悪さ2つ**: ①偽ピークが `kMaxPeaks`(24)を食い潰し、**高域の本物のピークが候補リストから溢れていた**(ターゲットのグローバルF2が 2456Hz → 修正後 1564Hz。真値の中央値≈1656なので修正後が正しい) ②見つからなかったバンドの**フォールバック値(前フレーム保持/既定値)が中央値に投票していた**。高い声ではF1フレームの大半がフォールバックなので、**中央値がフォールバックそのものになっていた**
  - **修正(dsp/VoiceAnalyzer.h)**: ①`extractPeaks` を**第1倍音より上から**探索(閾値の勘ではなく、そこから下は測定が存在しないという事実) ②極大判定を左側厳格 `>` に(平坦区間に曲率は無い) ③`assignFormants` が**バンド毎に「本物の候補を見つけたか」を返す**ようにし、フォールバックのフレームを統計から除外(F・L・relすべて) ④`rel` を「典型フレームの中央値」から「**観測できたフレームの中央値 × カバレッジ重み**」へ。⑤声道長推定(2パス目)も観測フレームのみで
  - **カバレッジ重みの定数は測定して決めた**(`kCovLo=0.10 / kCovHi=0.70`)。ground truth 210ケース(男女×5母音×7つのf0中心×3声道スケール、各ケース内でf0を9半音スイープ)で誤差をカバレッジ別に集計: **<0.10 は中央値5.24st(使い物にならない)/ 0.10-0.35 で1.70 / 0.35-0.70 で1.53 / >0.70 で0.70**。**なおカバレッジを完全に無効化しても後述の回帰は同じだったので、この重みは回帰の原因ではない**(切り分け済み)
  - **効果(ground truth、同一スイープでbefore/after)**: F1 全回答の中央値/p90 = **2.11/12.69 → 1.23/3.96 st**。`rel>=0.25` に限れば 0.57/1.27 → 0.46/1.15。**通常ピッチ(f0<=200)でも悪化なし**(F1 1.05/3.04 → 0.88/2.72、F2 0.34/7.53 → 0.28/7.46、F3 不変)。そして**測れない場合に数値を捏造せず「測定不能」を返す**ようになった(210ケース中4件)
  - **カタログの実測値がこう変わった**: F1 = 342〜601Hz(rel全部0.00、話者のf0より低いものあり)→ **774〜880Hz(rel 0.02〜0.45)**。小さい声道の開母音として妥当な値。**relのばらつきは欠陥ではなく正直な信号**: そし(f0 252Hz)は0.45、さら(f0 318Hz)は0.02。声が高いほどF1をf0から十分離して置けるフレームが減る
  - **【重要】この修正が下流に波及した — 母音分類が組み替わる**。F1は `classifyVowels` の入力なので、F1が変われば母音バケツの中身が変わる。合成の「男性160Hz→女性352Hz」ペアで**ソースとターゲットで共通する母音が3個→1個に落ち**、たった1母音に乗った結果 **formantが -0.37st(小さい声道なのに声道が長くなる=物理的にありえない)** になり既存テストが赤くなった。**対策(dsp/MatchingEngine.h)**: 母音マッチ経路を使う条件に **「共通母音が2個以上」** を追加。1母音は声道の測定ではなく1つの調音でしかない。**エンジンは元々このケースを `lowConfidence` と自己申告していたので、報告するだけでなく行動を変えた**という位置づけ。結果 +1.14st(真値+2.87、符号正・許容内)で全テスト復帰、しかも「diff vowels 150→352」の診断も 3.49→2.15st に改善
  - **`f*gain` もrelでゲート**(dsp/MatchingEngine.h)。`L=0` は「最強フォルマントと同じ大きさ」の意味なので、測れなかったバンドを無ゲートで比較すると**「欠けているバンドが最大音量」と読んで数dB要求してしまう**
  - **検証(test/offline_test.cpp に2件追加)**: (a2)**f0を振った既知F1の回帰** — 既存の(a)はケース内でf0固定なので、この-15.6stを検出できなかった。**実音声はピッチが動き、プロファイルは識別可能性が変動するフレーム群の中央値である**。契約は「常に正解」ではなく「**信頼できる(rel>=kMinRel)と報告したものは正しい**」+「**話者自身のf0より低いF1を自信を持って返さない**」。(a3)測れなかったバンドはゲイン補正を書かない
  - **既知の残課題(今回の対象外)**: カバレッジが高くても誤差が大きい尾がある(p90 6.26st)。これは**隣接フォルマントが1つの包絡極大に融合する**問題で、カバレッジとは無関係 — /o/(F1 450, F2 1035)がF2=2900(=F3を報告)、/a/ f0=310 が F2=1009(=F1を報告)。**relはピークが識別可能だったことしか言わず、正しいバンドに割り当てられたかは何も言わない**。固定探索レンジ+レベル最大選択という設計の限界で、既存の問題
  - **副作用として、極端な入力は「分析不能」を返しうる**: 高いf0(310Hz以上)で**単一の狭母音/i/を延々と持続**した音声は、偽ピークが消えた結果 `npk < 3` に引っかかってプロファイルが作れない(210ケース中5件、全部この形)。**実音声は複数母音を含むので該当しない**(4体とも866〜3018フレーム取れている)。`npk<3` ゲート自体は以前からあり、偽ピークが数を水増ししていたので発動していなかっただけ
  - **エンジン側にも同じ脆弱性が潜在**: `PsolaEngine.h` の極大判定も同じ非厳格 `>=`。ただしF1探索が250Hzからなので**通常の入力(f0 100〜200Hz)では平坦部と重ならず発症しない**。ユーザー自身が高い声(f0>250Hz)で使う場合のみ。**今回は触っていない**(bitexactで音の不変を確認済み)
  - **未着手**: /i/ /u/ の声道長からの補完(検討時の項目4)。ユーザー確認待ち

- **v0.37.0: 実測ターゲットキャラクター4体をマッチングのカタログに追加(うる/くら/さら/そし)**。ユーザー提供の実録音(48kHz mono、84〜165秒)を `VoiceAnalyzer` に通して測った**実測値**を `dsp/SampleTargetCatalog.h` に追加。従来の5体(Feminine Standard 他)は**理想値**であり、この2種類は性質が根本的に違うので混同しないこと。
  - **実測値なので `vow[]`(AEIOU別サブプロファイル)を持つ** → `MatchingEngine::autoSet` が**母音マッチ経路**を通り、AEIOUマップ(va_* 30項目)が実際に書かれる。理想値5体は `vow[]` が無いのでグローバル・フォールバック経路(これは設計通りで警告ではない)。`hnr[]`/`hfDb` も入るので **Air / Air Shine の自動設定も動く**(理想値5体では自己無効化される)
  - **4体とも `rel[0] = 0.00`**。f0が258〜368Hzと高く、F1が 2.5×f0 を下回るため**F1の位置は原理的に復元不能**(VoiceAnalyzer.h の識別可能性の議論を参照)。`F[0]` は**測定値のまま置いてある** — 話者自身のf0より低い値が出ているものがあるが、それがまさにこの状態の症状。**もっともらしいF1を手で書き入れてはいけない**: rel を見ないダウンストリームは全部それを信じてしまい、捏造した測定値になる。rel=0 なら該当バンドは寄与ゼロで、F1はグローバルformantと一緒に動く(物理的に正しい挙動)
  - **数値は手打ちしない**。`test/profile_dump.cpp` が生成器で、`profile_dump <rec>.wav --cxx <id> <En> <Jp>` がそのまま貼れるエントリを吐く。**カタログのブロックが再生成結果とバイト一致することを確認済み**。修正が必要なら再生成すること
  - **`VoiceAnalyzer::analyze` に `maxSec` 引数(既定20.0)を追加**。20秒という上限は**対話パス(Record/Load Target がメッセージスレッドで走る間ユーザーを待たせない)の予算**であって推定器の要求ではない。既定値のままなので**既存呼び出しの挙動は不変**、生成器だけが全長を測る。全長を測ると1442〜3820有声フレームになり、5母音すべてが有効フレーム数を満たす
  - **音声ファイルはリポジトリに入れていない**(従来方針どおりローカルのみ)。カタログに入るのは1キャラあたり数百バイトの統計量だけで、元音声は復元できない
  - **検証**: `offline_test.cpp` に (i)「built-in target catalog」節を追加 — 男性ソース(公表値F1-F3の合成発話)から全9エントリに対して autoSet を回し、①pitchが目標中央値(知覚バイアス -1st 込み)に着地 ②**formantが正**(男声→小さい声道で負が出るのが、この経路を作り直した原因の症状) ③データが支える経路を通っているか(実測=母音マッチ / 理想=フォールバック)④全書き込みが有限かつ範囲内、を確認。**ALL PASS**
  - **【既存バグ修正】`offline_test.cpp` の終了コードに `mFail` が入っていなかった** — Matching節は全部FAILを表示しても exit 0 だった。`(mFail + naFail + vaFail)` に修正。**失敗できない検査は検査ではない**
  - **UIレイアウトは実質無変更**。タイルは 5+1 → 9+1 の10個になるが、`cols` は幅650以上なら全数なので**行数1のまま・タイルサイズも96pxのまま**(最小ウィンドウ幅1180のとき利用可能幅1124pxに対して行幅 616→1032px)。オフスクリーン描画での確認は不要と判断した(この算術以外に変わる要素が無い)
  - **未確認**: 実声でのマッチ品質はユーザー評価待ち。オフラインの合成ソースでは f2shift が ±2st クランプに張り付くが、これは**合成テスト信号側のF2推定誤差(既存の既知の限界、per-vowel detection の worst F2 err 5.21st)**によるもので、素直な合成母音ソースでは -1.44st 程度に収まる

- **v0.36.14: バッジのふち撤去 / キャラ画像をプリセットへ / 画像選択をSAVEメニューへ / OUTPUTをLRランプに**。①バッジのレベル連動リムを削除(見えづらいとの判断)。`HeroCircle` はアニメーション無しになり、タイマーは4Hzで**画像パスの変化を見張るだけ**。②**キャラ画像がプリセットに入る**ようになった。`voxMorphPresetXml()` を新設し、**プリセットを書く4箇所(MAINバー/PRESETSタブ/MATCHING)を全部これに通した** — 書き手が増えても形式がずれない。読み込みは `voxMorphApplyPreset()` で `characterImage` 属性が**ある場合のみ**適用(v0.36.14以前のプリセットは画像を消さない)。③**画像選択をバッジのクリックからプリセットSAVEボタンのメニューへ移動**(クリックが煩わしいとの指摘)。FileChooserは `PresetBar` が持つ形にし、`HeroCircle` は表示専用になった。④**OUTPUTのレベルバーを左右2つのランプに置換**(`OutputLamps`)。消灯=無音 / ミント=出力あり / ピンク=そのチャンネルが0dBFSに到達。**ピンクは1.2秒ラッチする** — クリップは数サンプルで終わるので、ラッチしないと見た瞬間には消えている。**チャンネルごとに分けたのは、片チャンネル落ちが一番拾いたい障害だから**(合算バーだと隠れる)。**検証の注意**: ハーネスでエンジンに大きな信号を通すと**Auto-Muteが働いて出力が0になる**ため、ランプの3状態は `uiOutL/R.push()` に直接値を入れて確認した(ランプが読む値そのものなので、ランプの判定ロジックの検証としては十分)。プリセットのラウンドトリップは実ファイル経由で確認

- **v0.36.13: 円形バッジのビジュアライザーを撤去 + キャラ画像をローカルから選べるように**。①**ミニビジュアライザーを削除**(ユーザー判断「目立たず有効でない」)。**副次的にCPUが戻る**: バッジは全ページ常時表示なので `SpectrumData` のビューに登録している間はFFTとオーディオ側タップが常時走っていた。撤去で**Visualizerページを開いている間だけ解析する**という本来の規約に戻った(ハーネスで `uiWantsViz=0` を確認)。レベル連動のふち(halo)は残置 — 「音が出ているか」が一目で分かる唯一の要素なので。②**キャラ画像のユーザー設定**: バッジをクリック→ポップアップメニューで「画像を選択...」「既定の画像に戻す」。パスは `VoxMorphProcessor::characterImagePath` に持ち、`monitorDeviceName` と同じ流儀で**プラグイン状態のXMLに保存**(`characterImage` 属性)。**画像そのものではなくパスを保存する**方針も同じ理由 — 状態はマシン間を渡らない前提。**設計上の注意3点**: (a)**デコードできた場合だけパスを確定する**(壊れたファイルを選んでも次回起動時にバッジが空にならない)、(b)**`RectanglePlacement::fillDestination` で描く**ので任意のアスペクト比が歪みなく円に収まる(はみ出しは中央クロップ)、(c)**タイマーで `loadedFrom != characterImagePath` を監視**し、ホストが後から状態を復元しても自動で反映される。**検証**: 既定/カスタム画像/存在しないパス の3ケースを描画(存在しないパスは既定へフォールバック)、状態のラウンドトリップも確認

- **v0.36.12: ノブ本体の白を背景色に合わせた**(アセットのみ、コード無変更)。`ui_knob_base{,_pink,_yellow}.png` の**白い部分を `bodyFill`(#f3f5fb)へ**。実測で本体は #f1f3f9〜#f3f5fa となり、背景 #f3f5fb とほぼ同一。**単純な白→背景色の置換ではなく、明度に比例した写像**を使った: 各ピクセルの `mx = max(r,g,b)` を取り `out = bodyFill * (mx/255)` とすることで、**白は背景色に、影やグラデーションは相対的な暗さを保ったまま**移る(単純置換だと立体感が消える)。**目盛りの色は彩度で保護**: `sat = (mx-mn)/mx` が 0.10 以上なら原色のまま、それ未満は上記の写像へ線形ブレンド。これでピンク/イエロー/ブルーの目盛りと外周アークは無傷。**変換スクリプトはリポジトリに入れていない**(アセットは変換後の状態がソース)。元の白いアートが必要なら v0.36.11 以前の assets/ を参照。**スクラッチパッドは消えることがある** — この作業中に /private/tmp のクリーナがクローンとpytoolsを消したので、再クローン+`pip install --target ./pytools cmake ninja` から復帰した

- **v0.36.11: 数値欄の編集中の文字色を修正 + MAIN以外のタブをMAINのデザインに寄せた**。①**【バグ】数値欄をダブルクリックすると文字が白く見えなくなる**。原因は `Label::createEditorComponent()` が **`copyColourIfSpecified(*this, *ed, textWhenEditingColourId, TextEditor::textColourId)`** をしており、**LookAndFeel_V4 が `Label::textWhenEditingColourId` を「既定(ダーク)スキームの defaultText = 白」で指定している**こと。`TextEditor::textColourId` を設定していても、この転写に上書きされる。対策: `ToneLookAndFeel` で **`Label::textWhenEditingColourId` / `backgroundWhenEditingColourId` / `outlineWhenEditingColourId`** を設定(加えて選択色・キャレット色)。**編集可能なLabelを追加するときは必ずこの3つを見ること**。②**MAIN以外の3ページが全面に白いカードパネル(`paintCard`)を描いていた**のを撤去。MAINは v0.34.0 でパネルを廃止して単色 `bodyFill` になっているので不揃いだった。**さらにこのパネルは半透明(cardFill=0xc7ffffff)だったため、その下にあるバンドのバルジが透けて「キャラの下に灰色の円」が出るという実害があった** — 撤去で同時に解消。③**MAIN以外のページはバルジの下から開始**するようにした(`sidePages = pageArea.withTop(bulgeBottom + 12)`)。MAINは中央列だけを下げる方式だが、他ページは単一ブロックなので全体を下げる。従来はMatchingのタイルがキャラの襟に重なっていた。④見出しを **`ak::styleSectionHeading()`(15pt bold / headBlue)に統一**し、Matchingの見出しを大文字化(TARGET CHARACTER / MY VOICE / AUTO MATCHING)。⑤**VoiceTileButtonの配色をパレットへ**: 暖色寄りの白+ミント選択+緑がかったアイコン → 白 + `ak::line` + `ak::sidebarSel` 選択 + `ak::treeLine` アイコン + `ak::ctrlInk` 文字。**見出しアイコンは付けていない** — MAINのマークは項目ごとに意味を持たせており、既存アセットにMyVoice等に合うものが無く、無理に当てると逆に安っぽくなるため

- **v0.36.10: ミニビジュアライザーを「かわいい」方向へ再設計 + 検証手順の重大な訂正**。①**襟のミラー型リング(v0.36.9)を廃止し、ポートレート円の内側・下半分に沿ったやわらかいアーチへ**。丸キャップの太めのバー22本、**声の帯域だけ(約80Hz〜8kHz = column 44〜192)**を左→右に配置。**全帯域(20Hz〜20kHz)を使うとアーチの1/3が常に空になり左右非対称に見える**ため範囲を絞った。色は**ANOKOEロゴと同じ 青→ラベンダー→ピンク**のパステル、半透明で肩に重なる。襟は無地に戻した。②**【重大】オフスクリーン描画ハーネスでは `isShowing()` が false になり、`SpectrumData` は解析を行わない**(「誰も見ていないなら解析しない」規約が正しく働くため)。**v0.36.9の「リングが低域10px→高域2pxで減衰するのを確認した」という検証記述は誤り** — 実際には全バーが最短長のまま描かれており、私が色の濃淡をスペクトルと誤読していた。**対策**: ハーネスで `ed->addToDesktop(...)` して**実ピアを持たせる**。`uiWantsViz`/`uiWantsMeters`/出力ピークを印字して**解析が本当に走っているか毎回確認する**こと(走っていれば uiWantsViz=1)。**`isShowing()` に依存する表示(スペクトル・メーター・レベル)はピア無しでは一切検証できない**

- **v0.36.9: キャラクター差し替え + 円形バッジをミニビジュアライザー化**。①キャラアートをユーザー支給の新規イラストへ(`assets/ui_hero_character.png`、1254px→**512pxへリサンプルして収録**)。**円形の枠込みの絵**なので、`icon_png` を拡大配置していた従来のコードをやめ、ポートレート円にそのまま `fillDestination` で描く。②**AEIOUの%表示(と顔にかかる暗いプレート)を廃止し、ミラー型スペクトルリングに置換**。**顔の上には一切描かない** — バンドが膨らませている**襟(collar、kHeroRim=34px)の中だけ**に描く。内側(ミント)=入力、外側(ピンク)=変換後出力、**真上が20Hzで時計回りに20kHz**。フォルマントシフターなので「ピンクの山がミントより外へずれる」のが見えるのが要点。③そのため `HeroCircle` の**バウンズを kHeroR*2 から heroOuter*2 へ拡大**した。四隅がバンドのクリックを食わないよう **`hitTest()` で円内だけ**に限定してある(ツールチップは維持)。④**襟が34pxしかなく、長さだけでは5:1程度のレンジしか作れない**ため、**明るさ(alpha 0.22〜1.0)に形状を担わせる**のが効いた。レベル→長さは -54〜0dB、gamma 0.72。⑤**`SpectrumData` のビューとして登録している**ので解析のオン/オフ規約には乗るが、**このバッジは全ページで常時表示なので、実質 FFT とタップが常時走る**(メッセージスレッドで1フレーム1回のFFT + オーディオスレッドのリング書き込み)。**常時表示の位置にビジュアライザーを置く以上これは避けられないコスト**。Performance Modeのフレームレート抑制は従来どおり効く。**検証**: ハーネスで倍音60本+ノイズを**実際にエンジンに通して**描画し、リングが低域→高域へ減衰するのを目視+数値(低域10px→高域2px)で確認

- **v0.36.8: 細かな整理**(見た目のみ、DSP/APVTS不変)。①「AEIOU Shapes」チェックボックス→**「AEIOU」**。②**Noise Gate を ADVANCED から VOICE QUALITY へ移動**(Natural Jitterの下)。③**画面下の常設文言を削除**。`footer` ラベル自体は残す — `flashFooter()` が保存完了などの一時メッセージに使っており、既定文字列が空になるだけ。④**中央(FORMANT)列を8px狭く**: `kMidTrim`(8)の半分ずつを左右列の幅に足し、`w2 = usable - w1*2` で中央が差分ぶん縮む。⑤**本文左右に `kPageMarginX`(8) の余白**。④⑤は数値を `AnokoeTheme.h` の定数にしてあるので、調整はそこだけで済む。**装飾ラインは追従する**(カードの実矩形から座標を取っているため、列幅を変えても再フィットは不要)

- **v0.36.7: MONITORでデバイス設定が飛ぶバグ修正 + Fmt Character追加ほか**。①**【重要バグ】MONITORを押すと入力・出力デバイスの選択が両方外れる**。真因は2つ: (a)**Audio Settingsのモニター先プルダウンが `monBox.getText()` を保存していた**ため、未接続デバイスの項目「Foo  (未接続 / offline)」を選ぶと**装飾付きの文字列がデバイス名として保存**されていた。(b)**JUCEの `setAudioDeviceSetup()` は「デバイスが存在するか」を確認する前に `deleteCurrentDevice()` を呼ぶ**(juce_AudioDeviceManager.cpp、8.0.4で確認)。存在しない名前を渡すと**現在開いているデバイスを破棄してから "No such device" を返す**ので、入力も出力も未選択になる。対策: (a)`monNames[]` に**実デバイス名を並列保持**し、表示テキストは絶対に名前として使わない。(b)`vmSetOutputDevice()` で**呼ぶ前に現在のデバイス一覧と照合**し、無ければ何も触らずエラーを返す。さらに他要因で失敗した場合も**呼び出し前のセットアップを復元**する。**今後 `setAudioDeviceSetup` を使う時は必ず事前照合すること**。②VOICE QUALITYをADVANCEDとOUTPUTの間(3列目)へ移動。③名称変更: High Range Start→**High Pitch Roof**、High Pitch Amount→**High Pitch Keep**、High Fmt Amount→**High Fmt Keep**、Const→**Co Shift**、AEIOU Characterチェックボックス→**AEIOU Shapes**、Characterプルダウン→**AEIOU Character**。④**Fmt Characterプルダウン新設**(`fcharacter`、AudioParameterChoice、**既定 Custom**)。Formantノブの直下に置き、下の7項目(consonant + F1〜F3 shift/gain)をまとめて設定する。値は `src/FmtCharacterPresets.h`(**DSPは読まない、UIがパラメータに書き込むだけ**なので dsp/ ではなく src/ に置いた)。キャラ名はAEIOUと共通、値は別(AEIOUは母音間の差、こちらは声道全体)。**既定をCustomにしたのは互換性のため** — 既存セッションのスライダー値を、実際にはそうでない「Natural」と名乗らせないため。⑤**手動操作でCustomへ戻る仕組み**: `ParamRow::onUserEdit` を新設し、**ドラッグ開始・数値入力・リセットボタンでのみ**発火させる。**`slider.onValueChange` を使ってはいけない** — プリセット読込・ホストオートメーション・Fmt Character自身の書き込みでも発火し、選んだ瞬間にCustomへ戻ってしまう。`onDragStart` は既存の割り当てを**チェーン**して壊さないようにしてある(JUCE 8.0.4のSliderParameterAttachmentはListener方式でこのコールバックを使っていないが、保証はないため)。**検証**: ハーネスでプルダウンを Anime/Soft/Uni と切り替えて7パラメータの実値を確認、その後 `onDragStart()`(JUCEの `sendDragStart()` が呼ぶのと同じもの)を叩いてCustomへ戻ることを確認。`offline_test` 全PASS、dsp/ 無変更

- **v0.36.6: 単位表記・項目名・High Range/Low Limitのオンオフ・FORMANT並び替え**。①**単位と数値の間にスペース**(`setTextValueSuffix(" " + unit)`)。②**PitchとFormantに「st」と符号**を追加(`ParamRow::setSignedValue()`)。**符号は値ではなく整形後の文字列から判定する** — パラメータは0ちょうどでも生値がわずかに負で「-0.00」と出るため。③あわせて**「-0.00」問題を全行で修正**(先頭のマイナスの後ろが0だけならマイナスを落とす)。Pitchだけ綺麗になると他がバグに見えるので全行に適用。④項目名: Intonation Amount→**Intonation**、Intonation Pivot→**Center**。⑤**新規パラメータ `hienable` / `lowlimit`(AudioParameterBool、既定 true)**。UIはHIGH RANGE/LOW LIMITカードのチェックボックスで、下に `bracket()` でツリー線を引いた(High Range=3項目、Low Limit=1項目)。**エンジンは無改造**: `PsolaEngine` は元から「hiRangeHz≤20」「pitchFloorHz=0」をオフとして扱うので、`PluginProcessor` の `p.hiRangeHz = pHiOn ? pHiFreq : 0.0f` / `p.pitchFloorHz = pLowOn ? pFloor : 0.0f` で門を作っただけ。**既定trueなので既存セッション/プリセットは挙動完全同一**(hifreq=0・pitchfloor=0 の保存済み状態は元々オフなので、「オンだが量が0」＝従来と同じ)。**既定をfalseにしてはいけない** — hifreq=300 等を保存済みのユーザーが黙って無効化されるため。⑥FORMANTのF1〜F3を **Shift 3つ→Gain 3つ** の並びへ(カードは `add()` の呼び出し順に積むので、呼び出し自体を並べ替え+`bracket()`の引数順も更新)。**検証**: `dsp/` 無変更につき bitexact は自明、`offline_test` 全PASS。UIはハーネスで全23個の読み値をラベル付きでダンプし、単位のスペース・符号・並び順・0表記を確認

- **v0.36.5: 単位を数値欄の中へ移動**(見た目＋入力、DSP/APVTS不変)。ラベルの「(%)」「(Hz)」「(st)」「(dB)」が読みづらいというユーザー指摘への対応。**`ParamRow` のコンストラクタで表示名の末尾の丸括弧を解釈**し、ラベルは「F1 Shift」、読み値は「-0.00st」になる。読み値のある slider/knob だけが対象で、トグルやプルダウンの末尾括弧には触れない。**実装は JUCE の仕組みに乗せただけ**: `Slider::setTextValueSuffix()` を使うと `getTextFromValue()` が末尾に単位を足し、`getValueFromText()` が入力から単位を剥がす。**SliderAttachment は textFromValue/valueFromText を上書きするがサフィックスには触れない**ので、アタッチメント生成の**後**に `setTextValueSuffix()` を呼ぶこと。**入力時に単位を無視**するのは `Label::onEditorShow` で `getCurrentTextEditor()->setText(単位を除いた文字列)` + `selectAll()`(JUCEは editorShown を setText の後に呼ぶので上書きできる)。「2.00st」を選ぶと「2.00」が出て、3+Enterで「3.00st」に戻る。単位を付けて「3st」と打っても通る(`AudioParameterFloat::getValueForText` が先頭の数値を読む)。**APVTSのパラメータ名(ホストのオートメーション表示)は従来どおり単位付きのまま** — UI側の表示名とは別文字列。**検証**: ハーネスでコンポーネント木から `vmValue` ラベルを集め、`showEditor()`→エディタの中身確認→値を入れて `hideEditor(false)`→表示を確認、を8箇所で自動実行(単位あり/なし両方の入力、範囲外はパラメータ側のレンジで正しくクランプ)

- **v0.36.4: 装飾ラインをユーザーの赤線注釈に数値フィット**(見た目のみ、DSP/APVTS不変)。ユーザーが赤線を描いた画像(webp)を受領し、**赤ピクセルを抽出→中心線を取り→ベジェをRMS最小でフィット**した(目分量で合わせない)。赤の抽出は `r-(g+b)/2>35` かつ **`|g-b|<25`** が要点 — UI自体のピンク(青が高い)と金(緑が高い)を除外できる。画像のスケールはダークバンドの上下端(kHeaderH=94 / kBandH=158)から算出(1.6899、ウィンドウ幅1184相当)。**判明した3つの問題**: ①**制御点が固定長(34px/56px)だった**。PITCHの区間長は223pxだが**AIR/FORMANTは42px前後しかなく、ハンドルが区間より長い**ため曲線が自分の始点より手前へ膨らんで折り返していた → **区間長に対する比率**(`kHRadial`=0.19 / `kHFlat`=0.73)へ。1組の共通比率で3ルートともRMS 2.8px以内に収まる。②**マーカーへの接続点が弦方向だった**ため、線が円盤の横をかすめて**空中で途切れて**いた(FORMANTでは13px上で終端)。→ `markIn`/`markOut` として**円盤の左右**に接続し、両脚が水平に出入りするようにした。③**襟の角度**: FORMANT 208°→**188.5°**、AIR 142°→**132.4°**(赤線の端点が襟の円周上に乗る角度をそのまま採用)。あわせて `markT` を 0.66→0.546 / 0.39→0.397 / 0.62(据置)、**AIRだけ `markDy`=+18** を新設(AIRは襟y=276と見出しy=279がほぼ同高でこのままだと真っ平らになるため、マーカーを下げてS字にする、というのが赤線の指示)。区間2は両端が同高なら直線、そうでなければ**左右非対称のS**(0.36/0.64、a+b≤1で往復しない)。**検証**: 描画結果を注釈画像と同じ座標系にリサンプルして赤線を重ね、PITCH/FORMANT/AIRの3箇所を拡大確認

- **v0.36.3: スライダーのハートつまみを新アートに差し替え**(見た目のみ)。**輪郭線のハート→白い塗りのハート+下方向のソフトシャドウ**にデザインが変わった(ユーザー支給の28x26 PNG)。アセットは**リサンプルせず**32x32キャンバスへ整数オフセット(+3,+6)で再パックし、**ハート本体のbboxがキャンバス中心に来る**ようにした(支給画像は影のぶん下に余白が偏っており、そのまま入れるとつまみがレールより上にずれる)。本体を中心に置くと本体/キャンバス比が 0.719→0.625 に下がり画面上のハートが13%小さくなるため、描画ボックスを `kHeartH`(25) に分離(レールは `kTrackBarH`=19、当たり判定は `kTrackH`=22 のまま)。結果、**画面上のハートの大きさは旧アートと同じ**。`ui_slider_knob_heart_{pink,yellow}.png` は v0.36.2 から未使用のまま残置(旧デザインなので、使うなら新デザインで作り直しが必要)

- **v0.36.2: 装飾ラインの接続先と、スライダーつまみの共通化**(見た目のみ、DSP/APVTS不変)。①**縦の落下線をツリー線と同じ列に**: 従来は `min(見出し文字box.x, ラベルbox.x)` = カード左端+13 だったが、行のブラケットは行内 x=4 すなわちカード左端+13+4 に立つので**4pxずれていた**。`kTreeRail`(4)を新設し `Card::treeRailX()`(= kCardPadX + kTreeRail)を装飾線とブラケットの両方が参照する。②**見出しノードは「マークと文字」の両方**: 左から来る線は文字boxを狙うと**シンボルマークを突き抜けてしまう**(AIR列だけが左から来るのでAIRで発生していた)。`Card::titleNodeBounds()` を追加し、右から来る時は文字の右端、**左から来る時はマークの左端**に接続する。マーク幅は `kTitleIconW`(32)に定数化。③**ハートのつまみを全スライダー共通色に**(ユーザー指示「カラーは共通です」)。`toned("ui_slider_knob_heart", tone)` → `image("ui_slider_knob_heart_png")` 固定。トーン(pink/yellow/blue)はレールのフィル側だけが持つ。`ui_slider_knob_heart_{pink,yellow}.png` は**未使用になったがアセットは残置**(CMakeがassets/*.pngを全部BinaryDataに入れる仕様なので、消す時は両方まとめて)。**検証**: オフスクリーン描画で変更前後を差分(9578px差、x24-1012 / y271-819)、AIR見出し・FORMANT列・PITCH列を拡大比較

- **v0.36.1: MAINタブの色と線の仕上げ(見た目のみ、DSP/APVTS不変)**。①**見出し文字を全て `headBlue` に統一**(FORMANTのpink・AIRのgoldをやめた)。系統色はノブ/スライダーのトーンだけが担う。②**`tintedImage()` の重大バグを修正**: `Image::convertedToFormat(ARGB)` は**既にARGBなら同じオブジェクトを返し**、juce::Imageのコピーは**ピクセルを共有**するため、色替えのループが `image()` キャッシュの**元画像そのものを塗り替えていた**。実測(オフスクリーン描画で確認): ADVANCEDのマークが `#c9d2e7`(=treeLine、装飾マーカーが同じ `ui_mark_M_Advanced.png` を染めるため)、選択中ページタブのアイコンが `#e5eafe`(=bandInk、白いタブ上でほぼ不可視)に化けていた。`.createCopy()` を挟んで解決。**今後 `tintedImage` を使う時はこの共有に注意**。③**ADVANCED と VOICE QUALITY のマークを `markBlue`(#5c76bd)で染色**(VOICE QUALITYの星は素材が金色のため)。`Card::setTitle()` に第4引数 `iconColour` を追加、透明なら素材のまま。④**装飾ラインの水平化**(ユーザーが赤線で指示): (2)マーカー→見出しは**マーカーのyを見出しのyに揃える**ことで常に純粋な水平直線になった(従来はxもyも比率補間していたので数px斜めになり、FORMANT列では見出しの上から降りてくる曲がったスタブに見えていた)。襟からの落差は区間(1)のイージングが全部吸収する。(4)ラベル→ノブは**明示的な水平直線→曲線**の2段構成にし、第2ハンドルの長さを「ノブがラベルより上にある高さ×1.35」で頭打ちにした(これを超えるとハンドルが直線より下に落ちて途中でたわむ)。⑤**スライダーの横棒を細く**: `kTrackBarH`(19)を新設しレール/キャップ/フィルはこちらを使う。`kTrackH`(22)はハートのつまみとサムの当たり判定に残るので、**棒だけが細くなりつまみの大きさは不変**。**検証**: オフスクリーン描画ハーネス(juce_add_console_app + `createComponentSnapshot`)でエディタを1400x1080でPNG化し、変更前後をピクセル比較。ハーネス自体はスクラッチパッド限りでリポジトリには入れていない

- **v0.36.0: 装飾曲線を「ノード図」として作り直し(妥協なしの実装)**。それまでは座標を合わせにいっていたが、ユーザー指摘「**座標よりも文字やアイコンのどこから繋がっているかが重要。ノード図のようなもの**」を受けて考え方ごと変更。①**すべての線はノードの境界に接続し、ノードの内部は通らない**。ノードは 襟 / マーカーの円 / 見出しの文字ボックス / ラベルの文字ボックス / ノブの円 の5つで、各区間はノードの手前6pxで止まる。**ノード自体が線の区切りになる**ので、文字の上に線が重なることが原理的に起きない(これが「文字に線が重なる」の根治)。②**文字ボックスは実寸を測る**: `Card::titleTextBounds()` と `ParamRow::labelTextBounds()` が `GlyphArrangement::getStringWidth()` で描画幅を求めて返す。Labelコンポーネントの余白をノード幅に含めない。③**区間の性格**: (1)襟から**放射方向**に出て走行方向へイージング→マーカーの円周で終端 (2)マーカー→見出し: **可能な限り水平**(両端のyが1.5px以内なら直線、それ以外も見出しへは水平に到達) (3)見出し→ラベル: 両者が共有する**左端の列**を垂直に降りる直線 (4)ラベル→ノブ: 水平に出て**接線方向の弧1つ**でノブのリム(7時半)へ。直線と弧が接線連続でつながる。④**マーカー位置は列ごとの係数**(`markT` = 0.62 / 0.66 / 0.39)。PITCH列の経路は他の2本の約3倍長く、**共通の比率では3本とも合わせられない**ため(v0.35.3の妥協点を廃止)。襟の角度も列ごと(218°/208°/142°)。⑤線幅はツリー線と同じ1px。**この構造なら、行や見出しの文字が変わっても線は自動で正しい位置に付く**(座標のハードコードが無い)。

- **v0.35.3: 装飾曲線の円への接続位置を修正(セルフチェックの結果)**。**検証方法**: 赤線指定のスクリーンショットと自前レンダリングをそのまま重ねるとスケールが違って比較できないため、**ウィンドウ幅に対する比率**で測って突き合わせた(スケールが打ち消える)。円の中心と襟の半径を画像から実測してキャリブレーションに使用。**結果**: ①**垂直区間のx座標は3本とも誤差0px**(v0.35.2の修正は正しかった) ②**マーカーのx座標は誤差 -21〜+33px**(許容) ③**円への接続位置が最大のずれ**で、PITCH/AIRを**帯の下端のフィレット点**に付けていたのに対し、指定は**襟のもっと下(約7時半とその鏡像)**だった=約50pxのずれ。→ 接続を角度指定に変更(`onCollar(deg)`、PITCH 218°/FORMANT 208°/AIR 142°)し、誤差を4〜20pxまで縮めた。**注意**: 赤線の3つのマーカーは切り欠きからの距離比が 0.52/0.31/0.35 とばらついており(PITCH列だけ経路が2.7倍長いため)、**単一の比率では3本とも一致させられない**。現在は一律0.45で、3本とも誤差40px以内に収まる妥協点を採っている。厳密に合わせたい場合は列ごとに係数を持たせる必要がある。

- **v0.35.2: 装飾曲線のアンカー位置を実測合わせ + 線幅をツリー線に統一**。ユーザーの赤線指定を座標で測り直して修正。①**垂直区間の位置が最大の誤り**だった: `card.getX() - 10`(カードの外側)に引いていたが、正しくは**ラベル文字の左端**(`row->labelBounds().getX()` をエディタ座標へ変換)。カード外側だとPITCH列で画面端から14pxしかなく、指定より約35px外側にずれていた。水平区間は見出しの文字の裏を通ってこの列まで来る、という構造。②**マーカー位置**を「見出しと同じ高さの中点」から「**切り欠き→折れ点の直線上を45%進んだ点**」に変更。PITCH/AIRのように切り欠きと見出しの高さが近い列では水平線上に乗り、FORMANTのように見出しがずっと下にある列では斜めの区間上に乗る——赤線の指定はこの挙動だった(高さ固定では列ごとに破綻する)。③制御点も見直し: 区間1は**切り欠きから放射方向**(`from + (from - heroC) * 0.22`)に出て区間2の向きへ合流、区間2は折れ点へ**水平に到達**して垂直への曲がりを作る。④**線幅 1.4 → 1.0**(ツリー線が1pxの矩形塗りなので同じ重さに揃えた)。

- **v0.35.1: 装飾曲線を「区間ごとの独立したベジェ」に作り直し**(v0.35.0の実装が崩れていた件の修正)。**v0.35.0は5つのアンカーを1本のCatmull-Romスプラインで貫いていたのが誤り**だった。アンカー間の距離が大きいと、スプラインは滑らかさを保つために大きく膨らむしかなく、カードの外へ飛び出して画面端に張り付いていた。**修正方針(ユーザー指摘どおり)**: 「切り欠き→マーカー→見出し→ラベル→ノブ」の**各区間を1本ずつ独立した3次ベジェで描く**。制御点は区間ごとに手で置き、両端を数px手前で切る(`bezierLeg()` の `trimStart`/`trimEnd`)ので、アンカーで自然に途切れて「項目から項目へのホップ」として読める。各区間の性格: ①襟から斜めに出て水平に寝かせる ②見出しまでの水平区間(マーカーはこの線上・見出しと同じ高さに置く) ③カード外側の余白を垂直に下る ④ラベルから水平に出てノブの7時半のリムへ上がる。**この修正で v0.35.0 のオーバーシュートとクランプ回避策(1/8係数・x座標クランプ)は不要になり削除**。

- **v0.35.0: MAINページの再編 + 信号フローの装飾曲線**。見た目のみ、DSP/APVTS不変。①**項目整理**(ユーザー整頓案どおり): 左列=PITCH/INTONATION/HIGH RANGE・LOW LIMIT/VOICE QUALITY、中央=FORMANT、右列=AIR/ADVANCED/OUTPUT。下段の3枚組は廃止し**3列構成**へ。②**Robotize と Robot Pitch をUIから削除**(未使用と判断)。**パラメータ `robot`/`robotHz` は登録も既定値もDSPもそのまま**なので、これらを含む旧セッション/プリセット/オートメーションは従来どおり動く(UIから触れないだけ)。③**Noise Gate を VOICE QUALITY→ADVANCED へ移動**(トーンもyellow→blue)。VOICE QUALITY自体もblueトーンへ。④**レイテンシーのドーナツを廃止**し、`StatusView` を**OUTPUT内の2行テキスト**に作り替え(数値・しきい値・内訳はv0.17.0から不変)。⑤DETAIL...ボタンをCharacterプルダウンの**次の行**へ分離。アクションボタンは中央寄せ→**左寄せ**に。⑥**セクション見出しを系統色に**(`headBlue`/`headPink`/`headGold`)。⑦**信号フローの曲線(`paintFlowLines`)**: キャラの襟の切り欠きから「マーカー→セクション見出し→行ラベル→ノブ」を通る曲線を3本(Pitch/Formant/Airの処理順)。**Catmull-Romをキュービックベジェへ変換**して描画するが、係数は教科書どおりの1/6ではなく**1/8**にしている(1/6だとカードの外側へオーバーシュートして画面端に張り付く)。中間アンカーのx座標も22px以上にクランプ。マーカーは `ui_mark_M_Advanced.png` を `tintedImage` でツリー線色に染めたもので、下地を塗ってから重ねて線を途切れさせている。曲線はエディタの `paint()` で描く(カード側に背景が無いので下に敷いても見える)。座標は `getLocalPoint()` でカード/行のローカル座標から変換。**純粋な装飾でパラメータには一切触れない**。**検証**: Standalone/AU/VST3 実ビルド、プレビューハーネスで既定/最小サイズを確認しながら曲線のアンカーを3回調整。

- **v0.34.0: MAINページの整理(フラット化・行間圧縮・ツリー表示・PITCH/AIRの繰り上げ)**。見た目のみ、機能変更なし。①**セクションのカードを廃止**: `ak::Card::paint()` はもう枠も背景も描かず、見出しだけを描く。本文全体を**フラットな一色 `ak::bodyFill`(#f3f5fb)**にし、`paintPage()` のグラデーションも廃止。レイテンシー表示のカードも同様に外して統一した。②**行間の圧縮**: `kRowH` 36→**30**、`kToggleH` 30→**26**、`kTitleH` 40→**34**、`kKnobRowH` 124→**116**、カード下パディング 8→4、`preferredHeight` の余白 12→8。③**PITCH/AIR列を繰り上げ**: 従来は3列とも「キャラ円のふくらみの下」から始めていたが、円が塞ぐのは**中央列だけ**なので、`pageArea` は帯のすぐ下から始め、**FORMANT列だけ** `c2Top` ぶん下げる方式に変更(`layoutMainPage`)。左右の列がヘッダー寄りに上がり、縦の空きが減った。④**ツリー表示**: `ParamRow::Tree{none,mid,last}` を追加。`mid` は左端に縦線+ラベルへの横棒、`last` は縦線を中央で止めて L 字に閉じる。行は `kTreeIndent`(14px)ぶん右へ寄る。FORMANTの Const〜F3 Gain と、Character/AEIOU Amount の2グループに適用(`bracket()` ヘルパで連続行を一括指定)。**折りたたみ機能ではなく純粋な装飾**。⑤F3 Gain と AEIOU Character の間に空行(`addGap(14)`)。⑥**ヘッダー高さ 62→94**(1.5倍)。⑦**紺色背景の星を削除**。**検証**: Standalone/AU/VST3 を実ビルドし、プレビューハーネスで既定サイズと最小サイズを目視確認。

- **v0.33.1: ダーク領域の輪郭を整形(輪郭線の削除・円の拡大・接合部のフィレット)**。見た目のみ。①**輪郭線を全廃**: バンドのパスに引いていたヘアライン(`strokePath(shape, 1.0f)`)を削除。これが「ヘッダーと本文の境界線が紺色エリアを横断している」線と「キャラ円の外周のリング」の正体だった。縁は内側シャドウだけで表現する。②**円を拡大**: `kHeroR` 100→**112**(中央UI)、`kHeroRim` 24→**34**(紺色の円形範囲)。③**4箇所の接合部に正しいフィレット**を実装(`kHeroFillet`=34)。**注意: 「エッジと円の両方に接する円板をunionで足す」方式は誤り**(最初にこれをやって失敗した)。接する=1点で触れるだけなので、4つの独立した黒い塊が角に浮いて見える。正解は**接する円板の「弧」を1本の輪郭の一部として歩く**こと。実装は `ak::bandPath()` に集約: 帯の上下エッジそれぞれについて、エッジからr・キャラ円中心からR+rの位置にフィレット中心を置き(水平オフセット `dx = sqrt((R+r)^2 - (d+r)^2)`)、`エッジ直線 → フィレット弧 → キャラ円弧 → フィレット弧 → エッジ直線` の順に時計回りで1本の閉パスを構成する。JUCEの角度規約(0=真上・時計回り)に合わせ、フィレットは常に短い方の弧、キャラ円弧は常に時計回りになるよう角度を正規化している。円がエッジに届かない場合は単純unionへフォールバック。④既定 1400×1080 / 最小 1180×920(円が大きくなった分)。**検証**: Standalone/AU/VST3 を実ビルドし、プレビューハーネスで既定サイズと最小サイズを目視確認。

- **v0.33.0: ヘッダー/バンドの整理と「凹んだ」ダーク領域(ユーザー指定の第3案)**。情報量を減らして視線を中央のキャラクターへ集めるための調整。**DSP・APVTS・機能構成は不変**。①**削除**: ヘッダー左のアプリアイコン(中央にキャラがいるため視線が割れる)と、Undo/Redoボタン(Cmd+Z / Shift+Cmd+Z / Ctrl+Y のショートカットは残っているので機能は失っていない)。②**キャラ丸UIを簡素化**: 外周の5色AEIOUドーナツを撤去。残したのは円内のキャプション(AEIOU BALANCE + 各母音%)と、**出力レベルで明るさが変わるリム**だけ。派手さを減らしつつ「声で反応する」性質は維持した。③**ダーク領域の形をパスで定義**(`ak::bandPath`): 帯の矩形と、キャラ円より `kHeroRim`(24px)大きい円との**和**。非ゼロワインディングで合成されるので、**ヘッダーにも本文にも円弧どおりに食い込む**。ヘッダーの白帯は**エディタ側がバンドより先に描く**ようにし(`HeaderBar` は背景を描かずロゴだけ描く)、円のふくらみが白帯を正しく削り取るようにした。④**内側ドロップシャドウ**: パスの内側にクリップした状態で**同じパスを太さを変えて8回ストローク**すると、各ストロークの内半分だけが残り、縁に沿った柔らかい内側シャドウになる。**襟(円の周り)の幅 `kHeroRim` は内側シャドウの広がりより必ず広く保つこと**(狭いと両側からシャドウが重なって真っ黒になる。実際に一度そうなった)。深度用の縦グラデも**帯の矩形内だけ**に塗る(クリップ全体に塗ると、下にはみ出したふくらみがグラデ終端色=最暗色になり黒い襟に見える)。⑤**背景素材**: ユーザー提供の `UI素材背景.psd` を256pxのPNG(`ui_bg_tile.png`)に変換して同梱し、`juce::FillType(image, transform)` で**タイリング**(1セル≒118px)。⑥**タブを左右に分割**: Main/Matching/ASMR を左、Visualizer/Presets を右へ。⑦**プリセット行をバンド右上へ移動**し、ダーク配色+**Save/Deleteを文字からPNGシンボルへ**。高さは本文のプルダウンと同じ30px。⑧**`ak::tintedImage()` を追加**: マーク素材は濃紺なのでダーク背景で消える。アルファを保ったまま任意色へ置換したコピーを作りキャッシュする(未選択タブのアイコンとプリセットのSave/Deleteで使用)。⑨既定 1400×1050(画面にクランプ)、最小 1180×900。**検証**: Standalone/AU/VST3 を実ビルドし、プレビューハーネスで**5回描き直しながら**寸法と明度を調整した(襟の黒つぶれ、アイコンの視認性、縦の収まり)。

- **v0.32.0: モダン/かわいい方向へのスキン更新(ダークバンド+キャラクター丸UI)**。ユーザー提供の第2案イメージを反映。**DSP・APVTS・機能構成はすべて不変**(UIの配置と描画のみ)。①**ダークバンド**: ヘッダー直下に濃紺のバンド(`ak::paintBand`)。縦グラデ+菱形ラティス+星を**すべて手続き的に描画**(追加素材ゼロ、どの幅でも破綻しない)。星は `juce::Random rng(0x5eed)` の固定シードなので**再描画のたびにチラつかない**。②**キャラクター丸UI(`HeroCircle`)**: バンドの下端をまたいで配置し、ヘッダーと本文の両方に少し重なる(イメージどおり)。中身は `icon.png` を円にクリップ(素材が角丸スクエアのアイコンなので**1.16倍に拡大して四隅を円の外へ逃がす**)。周囲のリングは**AEIOU母音バランス**を5色の弧で表示=VISUALIZERページの母音ドーナツと同じ測定値(`VowelAdaptiveWarp::anchorWeights`)。**さらに外側のハローは出力レベルに追従**するので、AEIOU Characterがオフでも円は声で呼吸する。キャプションは円の**широ広い位置に弦(chord)として置き**、下端に置くと幅が足りず5項目が切れるため上方向へ移動+左右に0.30R のインセット。プレートは上から下へのグラデでフェードさせ、顔を横切る硬い線が出ないようにした。③**ナビをサイドバーから上部タブへ**(`ak::TabButton`): バンド下端に角丸トップのタブが並び、選択中は白=下の本文シートが手前に来たように見える。サイドバー(132px)が無くなった分、カードグリッドが横に広がった。④**プリセットバーをバンド右端へ移動**(右カラムから外れたので AIR / VOICE QUALITY が縦に余裕を持った)。⑤既定サイズ 1400×1030(画面サイズにクランプ)、最小 1180×900。**検証**: Standalone/AU/VST3 の3ターゲットを実ビルドし、`scratchpad/uiprev` のプレビューハーネスで**Main/Visualizer/最小サイズを目視確認**しながら寸法を4回調整した(円の大きさ・キャプション位置・キャラの収まり・縦の詰め)。

- **v0.31.2: UIを「ANOKOE」スキンへ全面改修**(ユーザー提供の完成予想図+HTML試作 `ANOKOE_UI_Offline` に準拠)。**DSP(`dsp/`)は1行も変更なし。APVTSのID・レンジ・既定値も不変**なので既存プリセット/セッションはそのまま読める。**v0.31.0/v0.31.1の上にリベースしてマージ済み**=別セッションの成果は失っていない(`uiWantsMeters` の公開と破棄時クリア、`vmDrawHz` による自律レート調整、Legacy Low LatencyのBETA移動をすべて引き継いだ)。①**素材の同梱**: `assets/` に45個のPNGを収録し、CMakeの `juce_add_binary_data(AnokoeAssets)` でバイナリに焼き込む(`file(GLOB assets/*.png)` なので**素材を置くだけで増やせる**)。1024pxのicon/logo/volume素材は表示サイズに合わせて縮小し合計472KB。参照は `BinaryData::getNamedResource()` の**文字列引数**なのでシンボル名をハードコードしていない。②**新規2ファイル**: `src/AnokoeTheme.h`(styles.cssの色・寸法・ノブ角度を定数化+画像デコードキャッシュ+`paintCard`/`paintPage`)と `src/AnokoeWidgets.h`(`ToneLookAndFeel`=ノブ/スライダー/チェックボックス/ボタン/コンボの画像描画、`IconButton`/`Card`/`SidebarButton`/`StatusButton`)。**色とアートを変えるならこの2ファイルだけ**触れば済む。③**ノブ**: `juce::Slider` の `RotaryHorizontalVerticalDrag` + `setRotaryParameters(1.25π, 2.75π)` に `drawRotarySlider` を差し替え、base画像→ring画像を扇形クリップ→pointer画像を回転の3枚重ね(HTML試作のcanvasクリップと同一)。**Sliderのままなので数値入力・ダブルクリック初期化・ホイール・ホストオートメーションが無改造で効く**。④**スライダー**: 左右キャップ+中央トラック+fill+ハート型サム。⑤**レイアウト**: TabbedComponentを撤去し、**ヘッダー(ロゴ/Undo/Redo/Monitor/Mute/Plugin/歯車)+左サイドバー5ページ(Main/Matching/ASMR/Visualizer/Presets)**へ。MAINは3列カードグリッド(左=PITCH/INTONATION/ADVANCED、中=FORMANT、右=プリセットバー/AIR/VOICE QUALITY、下段=HIGH RANGE・レイテンシードーナツ・OUTPUT)。⑥**ドーナツ3種の配置**: **VISUALIZERページに収めた**(BETAへの退避は不要だった)。上段に直線スペクトラム、下段に円形スペクトラム/AEIOU母音率/入出力レベルを横並び。⑦**項目の移動**(機能は不変): Noise Gate→VOICE QUALITY、Pitch Floor→HIGH RANGE(完成予想図どおり)。**GCI/Breath/Legacy Low LatencyはBETAウィンドウに残した**(完成予想図はADVANCEDにGCIを描いているが、v0.30.0の直接指示とv0.31.0のBETA移動を優先)。ADVANCED=Low Voice/Performance Mode/Stereo+BETAボタン。⑧**ウィンドウ**: 最小1240×860(ユーザー了承済みの「大きめ」)、既定1380×940だが**画面サイズにクランプ**。MAINはViewportでスクロール可。⑨**ロック機構**: 各行が `refreshLock()` を持ち、エディタの3Hzポーラーが一括で呼ぶ方式に統一(v0.30.0の行ごとタイマーを廃止)。ロック中は淡ピンク背景+PNGの錠アイコン(絵文字は環境によって描画されないため置換)。**検証(重要・今後も同じ手順を使うこと)**: **ローカルにcmakeを導入し(`python3 -m pip install --user cmake`)、Standalone/AU/VST3の3ターゲットを実ビルドして成功を確認**。さらに `scratchpad/uiprev`(リポジトリ非収載の**UIプレビュー用ハーネス**。実プロセッサ+エディタを短命ウィンドウに載せてタイマーを回し、`createComponentSnapshot` をPNG出力する小さなGUIアプリ)で**全5ページと最小サイズを目視確認しながら実装した**。JUCEを浅くクローンし `-DFETCHCONTENT_SOURCE_DIR_JUCE` で使い回すとconfigure 20秒/ビルド35秒。**UI作業では構文チェックだけでは足りない**(レイアウト崩れが検出できない)ので、このハーネスを再作成すること。**未対応・既知の残り**: MATCHING/PRESETS/ASMRの3ページは配色とカード枠だけANOKOEに合わせた最小対応で、中身のレイアウトは旧デザインのまま(機能は全て動作)。DETAIL/BETA/Audio Settings/Pluginsの各サブウィンドウも配色のみ追従。アプリ表示名は `PRODUCT_NAME` がVoxMorphのままでUI上の表記だけANOKOE(改名手順はこのファイル末尾の節を参照)

- **v0.31.1: スペクトラム表示タップのデータ競合を解消(コードレビュー指摘 Medium)**。指摘内容=「`SpectrumData::timerCallback()` がUIスレッドから `proc.vizIn`/`vizOut` を直接読む一方、オーディオスレッドは同じリングへ書き続けている。`vizPos` の release/acquire は読み始める位置を伝えるだけで、UIが4096サンプルを読んでいる最中の上書きは防げない=C++メモリモデル上のデータ競合」。**指摘は正しく、修正した**(再現もさせた。下記検証(a))。**採った方式=指摘の推奨2「シーケンスカウンタ」+サンプル自体のatomic化**。ロックもオーディオスレッド側のスナップショットコピーも増やさない。①**リング要素を `std::vector<std::atomic<float>>` にし、両側とも `memory_order_relaxed` で読み書き**。relaxedのfloat load/storeはビルド対象の全プラットフォームで単なるmov1命令なので、これだけで「競合ではあるが実害が出にくい」が「そもそも競合ではない」に変わる(TSanも黙る)。ロックへ退化しないことは `static_assert(std::atomic<float>::is_always_lock_free)` で固定。②**`vizPos` が世代番号を兼ねる**: UIは読む前に `pos` を取り、**INとOUTの両方を同じ `pos` に対して**ローカル配列(`winIn`/`winOut`)へコピーし、コピー後にもう一度 `vizPos` を読んで `vizPos - pos > kVizLen - kN` ならそのフレームを捨てる。書き手は `pos` から前へ進むので、UIが読んだ最古のサンプルに追いつくまでに `kVizLen - kN` = **12288サンプル(48kHzで256ms)** の余裕がある=**通常のフレームが捨てられることは無く**、メッセージスレッドが0.25秒止まった時だけ1フレーム飛ぶ(次の33msで復帰)。③ついでに**INとOUTが別々の `vizPos` を読んでいた**のも直った(2本のカーブが最大1ブロックずれた瞬間を描くことがあった)。④**`vizPos` を `int`→`unsigned` に変更**: 符号付きのままだと2^31サンプル≒**12.4時間**連続稼働で符号付きオーバーフロー(UB)。unsignedならラップが定義済みで、差分も2^32(約25時間)をまたいで正確。**DSP(`dsp/`)は1行も触っていない**(音は完全に不変。表示タップのみ)。
  **検証**: (a)****`test/viz_race.cpp` を新設**し、書き手/読み手の実コードを抜き出して、ThreadSanitizerで修正前後を比較**: **修正前=データ競合4件を報告+読み手を1ms止めると30フレームが「2つの時点が混ざった窓」になった**。**修正後=TSan警告0件・混線0件**。実レート再現(512サンプルを10.7ms毎に書き、30Hzで読む×2秒)では**破棄フレーム0**=表示は従来どおり滑らか。2^32ラップ直前から回すケースも混線0。(b)**オーディオスレッドのコスト計測**: 512サンプルブロックあたり plain float 38ns → relaxed atomic 263ns(ベクトル化が効かなくなるため約7倍)。ただし**絶対値は0.26µs=10.67msブロック予算の0.0025%**で、しかもビジュアライザーが画面に出ていなければv0.31.0の需要ゲート(`uiWantsViz`)で丸ごとスキップされる。(c)AU/VST3相当・スタンドアロン相当の両構成を `-Wall -Wextra -Wshadow` で通し、**警告テキストが変更前とdiff一致(新規警告ゼロ)**。(d)`dsp/` 無変更のため offline_test / bitexact は非実行。

- **v0.31.0: Performance Mode 新設 + 旧Low LatencyのBeta移動 + ゼロ値バイパス監査(ChatGPT指示書ベース)**。目的=**音質を1ミリも変えずに**小さいデバイスバッファ(64/128/256)を実用にすること。①**Performance Mode(新パラメータ `perfmode`、既定オフ)**: `PsolaEngine` は**このパラメータを一切読まない**(processBlockで `uiPerfMode` へ publish するだけ)。効果は(a)描画専用タイマーの更新頻度を2/3へ(SpectrumDataの4096点FFT×2、母音/レベルドーナツ、OutputMeter、SonarPad、StatusView)(b)スタンドアロンのAudio SettingsにPERFORMANCE節(バッファ調整)を出す、の2点のみ。**「遅延が下がるスイッチ」ではないと UI 文言に明記**。②**旧Low LatencyはBETAウィンドウへ移動し表示名を「Legacy Low Latency」に**。**パラメータID `lowlat` ・範囲・既定値・DSPは完全に凍結**(旧プリセット/セッション/オートメーション互換)。DSP実装(先読み半減・追跡下限90Hz・グレイン上限縮小・Air FFTクリーンアップ無効)は1行も触っていない。両モードは独立で同時ONにできる。③**ゼロ値・OFFバイパスは全モード共通**(Performance Mode限定にしない=同じ出力になる処理を任意にする理由が無い): エンジン側=`mix==1.0` でdryリング読み出しをスキップ(`1.0f*wet+0.0f*dry` は IEEE で wet と完全一致)/Natural Airが静まりD+窓ぶん排出しきったら noiseBuf クリア・processAirFxのFFT段・air再加算を丸ごと停止/AEIOU amount 0 のとき30floatマップ複製を省略。プロセッサ側=**表示タップの需要ゲート**(`uiWantsMeters`/`uiWantsViz`。エディタ非表示・タブ非表示なら4本のレベルメーター走査と2本のビジュアライザーリング書き込みを完全停止=**プラグインでウィンドウを閉じていれば無料**)/Auto-Muteオフ時はRMS走査ごとスキップ(状態はリセットして再ONで誤爆させない)/Output Gainが0dBかつ平滑収束後は乗算ループ停止(誤差<1e-6=-120dBでスナップ)/ASMR位置が中央かつ収束後は `std::copy`/FXスロット0本なら try-lock すら取らない(`fxCount` atomic)。④**スタンドアロンのバッファ支援**: Audio Settings に「Optimize buffer」(64→128→256を順に試し、デバイスが持つ最初のサイズを採用。**再初期化に失敗したら元の設定へ自動復帰**)と「Restore」(直前のサイズへ手動復帰)。**Performance ModeのON/OFFがデバイス設定を勝手に書き換えることは無い**(押した時だけ)。プラグイン版はホストのバッファに一切介入しない。⑤**エンジン先読みの短縮は今回やっていない**(指示書どおり別課題)。

  **検証**: (a)**`test/bitexact.cpp` を新設**して12構成(ピッチ±・air・shine・低音・LowVoice・Legacy LowLat・robot・tilt/jitter・breath/GCI・vowel adapt、ブロック32〜512)の**全出力サンプルを変更前後で `cmp` し完全一致**を確認。**この確認が今回の主たる根拠**(offline_testは挙動と品質を見るもので、「サンプルが動いていない」は見ない)。dsp/ を触る最適化は今後もこれを使うこと。(b)offline_test 全PASS、**出力テキストは新規1行を除き変更前とバイト一致**。(c)AU/VST3/Standalone相当を `-Wall -Wextra -Wshadow` で通し**新規警告ゼロ**(pristineとdiff一致)。

  **途中で踏んだ罠(重要)**: air の遊休バイパスを入れた最初の版で**リングバッファ1周(32768サンプル≒0.68秒)前の古いエアが再生された**。遊休中に noiseBuf/noiseFx のクリアを止めると、復帰時に「読み出し位置=書き込み位置-D」の区間だけ誰もクリアしていない状態になるため。対策=**遊休から復帰する瞬間に D+窓ぶんだけ明示的にクリアする**(`airActive && airTail == 0` の分岐)。実測で最大3.1e-2、その地点の正規信号が2.4e-2=**局所的に本来の音より大きい**レベルだった。この差は bitexact.cpp でしか出せず、offline_test に入れようとした3種類のアサーション(ブロックサイズ非依存/無音区間の残留/履歴の異なる2エンジン比較)は**どれも検出できなかった**ので、offline_test 側には「無音は無音のまま」までを入れ、限界をコメントに明記してある。**なおこの調査で分かった既存事実: エンジンの出力はブロックサイズを変えるとサンプル一致しない**(air完全オフでも最大1.26。初回ピッチロックのタイミングがずれてグレイン格子の位相が恒久的にずれるため)。HANDOVERの「全挙動バッファ非依存」は**検出周期・スケジューリング・デジッパ等の挙動**の話であって、ビット一致の主張ではない — 今後ブロックサイズ間の比較をテストに使わないこと。

  **ユーザー実機確認の重点**: Performance Mode ON/OFFで音が変わらないこと(特にPitch/Formant/Natural Air)、64/128/256での安定性と実遅延、Optimize buffer 後の音切れ有無とRestoreで戻せること、Legacy Low Latencyが従来どおり動くこと、旧プリセット/旧セッションが読めること、Analyzer/Visualizer表示中と非表示時のCPU差、DAWでウィンドウを閉じている間のCPU差。

- **v0.30.4: レベルドーナツをL/R左右分割レイアウトへ変更(ユーザー指定)**。同心4リング(外→内に inL/inR/outL/outR)をやめ、**左半分=Lチャンネル・右半分=Rチャンネル、各半分に2列(外側=入力ミント / 内側=出力ピンク)**に再構成。**両半分とも下から12時へ向かって伸び、12時がスケール最大**(ユーザー指定)。左右が鏡写しになるため、**パンやマイクの左右差が非対称としてすぐ見える**(ASMRパン時の見え方をSVGで確認)。実装は `angleAt(side, p)`: 左半分は `π+kGapB` から時計回り、右半分は `π-kGapB` から反時計回り、どちらも `π-kGapB-kGapT` のスパン(JUCEの角度は0=真上・時計回り。`addCentredArc` は from>to でも逆回りに描いてくれるので右半分もそのまま渡せる)。**下側のギャップ kGapB=0.17 は上側 kGapT=0.10 より意図的に広い**: 最初 0.07/0.07 で描いたらL/Rの弧が下でつながって1本のU字に見え、2つのメーターと認識できなかったため。色は**L/Rが位置で区別できるようになったので濃淡バリエーションを廃止**し、入力ミント/出力ピンクの2色のみに整理。中央パックは「IN/OUT」から**「L | R」**表記へ変更(半分がどちらのチャンネルかを直接示すため。入力/出力の区別は色とツールチップが担う)。0dBFS基準線とレッドゾーンは左右それぞれに描画。**検証**: `dsp/` 無変更。AU/VST3相当・スタンドアロン相当の両構成でエラー0

- **v0.30.3: 入力L/R・出力L/Rの4レベルを1つにまとめたドーナツ型インジケータを追加**。VISUALIZER行が**直線グラフ+ドーナツ3枚**に(`VisualizerRow` は可変個の`initializer_list`受け取りに変更)。既定ウィンドウ幅680→**800**、最大1100→1300。①**計測の共通化**: `VoxMorphProcessor::LevelMeter`(atomic rms/peak、RMSはattack20ms/release250ms、peakは450ms減衰ホールド、いずれも**時間定数なのでバッファサイズ非依存**)を新設し、`uiInL/uiInR/uiOutL/uiOutR` の4本に。**v0.30.0の`uiOutRms/uiOutPeak`は廃止**し、OUTPUTセクションの横バーは `max(L,R)` を表示するよう変更(単一バーのメーターとしてはこちらが正しい)。②**測定位置**: 入力は processBlock 先頭の**ノイズゲート・Pre FXより前**(ゲートが閉じている間もマイクの状態が見えるように。この時点では `buffer` が未加工の入力を保持している=変換は monoScratch/scratchL/scratchR 側で行われるため安全)。出力は最終 buffer(mute・gain・ASMRパン・Post FX通過後)。モノラルバス時は各ペアの2本が同じ値になる。③**表示**: 同心4リング(外→内: 入力L=ミント / 入力R=ミント濃 / 出力L=ピンク / 出力R=ピンク濃)。**最初に全周360°で実装したが却下**した: 始点も終点も0dB位置も無いため「クリップまでどれくらいか」が読めず、RMSより先行するピーク目盛りが空きトラック上の意味不明な線に見えた。→ **下部を開けた270°ゲージ**(7:30開始・時計回り、-60〜+6dB)+**0dB以降のレッドゾーン塗り**+**4リングを貫く0dBFS基準線**に変更。ピークは各リング上の放射目盛りで、0dBFS到達時は赤。**検証**: `dsp/` 無変更(offline_test非実行)。AU/VST3相当とスタンドアロン相当の両構成を `-Wall -Wextra` で通し新規警告ゼロ。JUCEソースで `Path::addCentredArc` の角度規約(0=真上・時計回り)を確認済み。SVG出力の描画検証(通常発話/小音量/出力クリップ/ミュート中)で全周版→270°版の判断を行った

- **v0.30.2: AEIOU母音率ドーナツをビジュアライザーに追加**。VISUALIZER行が**直線グラフ+スペクトラム・ドーナツ+母音率ドーナツ**の3枚並びに(`VisualizerRow`、各ドーナツは幅=row×0.26で縮小可)。既定ウィンドウ幅を560→**680**、最大900→1100へ(3枚分の余白確保。狭くしても消えない)。①**データ源の判断がこの版の要点**: 母音率は**新たに推定していない**。エンジン内 `VowelAdaptiveWarp` が持つ母音座標(height=F1由来の開口度 / frontness=log(F2/F1))を `VowelAdaptiveWarp::anchorWeights()`(**v0.30.2で追加した公開staticヘルパ**)に通した、**AEIOU Character が実際に効かせている混合比そのもの**を表示する。ビジュアライザーのスペクトラムから母音を独立推定する案は**却下**: 検証されていない第2の推定器がDSPと食い違う構図になり、v0.28.3/v0.28.4で2度はまった罠そのものだから。②**制約(ユーザーに明示済み)**: エンジンは `vaOn`(AEIOU Character ON かつ Amount>0)のときしか `updateVowelAdapt()` を呼ばない=**機能オフでは母音データが存在しない**。常時トラッキングさせるにはスペクトル層を常時起動する必要があり、表示のためだけに約+0.7%/コアのCPUを恒常的に払うのは「未使用機能は自動スキップ」方針に反するため**採用せず**、UI側は「AEIOU Character is off」と表示する。常時表示にしたい場合の変更点は `PsolaEngine` 478行の `if (vaOn)` を広げるだけ(CPU増を了解の上で)。③**スレッド安全**: エンジンのデバッグgetterはメッセージスレッドから直接呼べないため、processBlockで `uiVowelH/uiVowelF/uiVowelConf/uiVowelActive`(atomic)へ publish して UI が読む。④**表示**: 角度が比率に比例する真のドーナツチャート(`Path::addPieSegment`、JUCEは0=真上・時計回りなのでそのまま使える)。5色(A=ピンク/I=ミント/U=ラベンダー/E=アンバー/O=スカイ)、中央パックに最優勢母音と%。UI側で0.22の追従平滑。**無声時は最後の分布を保持したまま薄く**フェード(固まった形を生きた値と誤認させないため)。⑤**`anchorWeights()` は process() 内の重み式を意図的に再掲している**: process() は「重み×マップを累積して最後に総和で割る」形なので、正規化を先にやる共通化に置き換えると最下位ビットが変わる。DSPに一切リスクを取らないため関数を分け、隣接配置+コメントで同期を促している。**検証**: `offline_test` 全PASS(Vowel-Adaptive Warpの **off/amount-0 bit-identical** 含む=dsp追加が無害であることの確認)。加えて `vowel_check.cpp`(リポジトリ非収載)で**日本語5母音の代表フォルマントを声道スケール×1.00/×1.15/×0.88で入力し、15/15で正しい母音が最大**(該当母音のシェア50〜84%)になることをグラウンドトゥルース確認した

- **v0.30.1: ビジュアライザーにドーナツ(レーダー)版を追加**。ユーザー提供の完成イメージ(円形・中央に白い突起・下にLatency行)に合わせ、MAINタブのVISUALIZER行を**左=従来の直線グラフ / 右=ドーナツ版**の横並び(`VisualizerRow`)に変更。行の高さ168はそのまま、ドーナツは正方形で幅は`row幅×0.36`(狭い時は消さずに縮む)。①**FFTは共有**: 解析部を `SpectrumData`(30Hzタイマー+2本の4096点FFT)に切り出し、`SpectrumView`/`RadialSpectrumView` は**描画専用**に。ドーナツが独自に解析すると同じ数値のためにFFTが倍になるので必ず共有すること。どのビューも画面に出ていなければ解析ごとスキップ。ビュー登録は `SafePointer`。②**設計上、直線グラフと意図的に変えた2点(合成スペクトラムをSVGに出して検証済み)**: (a)**周波数レンジは60Hz〜12kHz**(20Hz〜20kHzではない)。対数軸だと**20〜110Hzの空っぽな帯域が円の最初の1/4を占領**し、フォルマントが下半分に押し込まれて完全に不格好になる。実際に3レンジを描画比較して決定。(b)**220列を72点へ平均+円環3タップ平滑**してから**中点アンカーの二次ベジェ閉曲線**で描画。220列をそのまま幅50px程度の円環に巻くと**倍音1本1本が針**になり、完成イメージの丸い星型にならない。閉曲線は中点アンカー方式なので12kHz→60Hzの継ぎ目が滑らかにつながる。③半径は `-66dB(内周)〜+6dB(外周)`(直線グラフと同一スケール)、ただし**下端に6%のマージン**を入れて無音帯が中央パックに乗り上げないようにした。穴の半径=外周×0.30、中央パックは `DropShadow` で浮いて見せる。ミント=入力/ピンク=出力の色は従来どおり。**検証**: `dsp/` 無変更。同じ計算式をSVG出力する検証プログラム(`donut_preview.cpp`、リポジトリ非収載)で合成母音スペクトラムを描き、レンジ・点数・穴径を比較して決定。実声での見え方は未評価

- **v0.30.0: メインタブ改修(MUTE/MONITOR、Audio Settingsウィンドウ自作、BETAウィンドウ、プリセットバー、出力メーター)**。DSP(`dsp/`)は1行も変更なし。①**MUTE / MONITOR(スタンドアロンのオプションバー)**: `Plugins... | Audio Settings... | MUTE | MONITOR` の並び。状態は**APVTSパラメータではなく** processor の `std::atomic<bool> muted/monitoring`(プリセットやUndoが勝手にミュートを解除しないため。起動時は常に非ミュート)。**相互作用の仕様**: MONITOR ON→出力デバイスをモニター用に切替+MUTEも自動ON / MONITOR OFF→元のデバイスへ戻すが**MUTEはONのまま**(モニター終了だけで不意に配信復帰しない安全動作) / MONITOR中にMUTE OFF→「配信に戻る」意思とみなしMONITORもOFF。**重要な設計判断**: モニター中は `muted && !monitoring` の条件で**MUTEが音を消さない**(出力先がモニター機に切り替わっており、消すとモニターの意味が無くなるため。MUTEの点灯は「モニターを抜けたら無音に着地する」印)。実装は processBlock の既存 auto-mute の `target` に論理和で合流(専用の平滑器を増やさない)。デバイス切替は `StandalonePluginHolder::getInstance()->deviceManager` の `AudioDeviceSetup.outputDeviceName` だけを差し替え(入力デバイス・SR・バッファは不変)。切替前のデバイス名は `proc.preMonitorDeviceName` に退避。FxBarの4Hzタイマーが「monitoring中なのに現在の出力先がモニター機でない」状態を検出したら自動でモニター解除(Audio Settings側で手動変更/デバイス切断への保険)。②**Audio Settingsウィンドウを自作**(`AudioSettingsPanel`/`AudioSettingsWindow`): JUCEの `showAudioSettingsDialog()` は差し込みができないので、同じ `AudioDeviceSelectorComponent` を自前ウィンドウでホストし、**モニター出力デバイス選択欄**と、ADVANCEDから移設した **Auto-Mute on Feedback** を同居させた。モニターデバイス名は `getStateInformation` のXML属性 `monitorDevice` で永続化(属性が無いstateは既存の選択を壊さない=マシン固有設定のため)。未接続のデバイスは「(未接続 / offline)」付きで一覧に残す。③**BETAウィンドウ**(`BetaPanel`/`BetaWindow`): ADVANCEDに `BETA...` ボタンを追加し、**GCI Grain Sync と Breath をここへ移動**。④**MAINタブ先頭にPRESETバー**(`PresetBar`): プルダウンは**選択した時点で適用**(PRESETSタブの select→Load とは意図的に違う)。Saveは PopupMenu で「上書き / 別名で保存」を選択、別名は AlertWindow で名前入力。Deleteは NativeMessageBox で確認。⑤**OUTPUTセクションに出力レベルメーター**(`OutputMeter`): processBlock 末尾で**完成後のbuffer**(mute/gain/ASMRパン/Post FX/ターゲット再生をすべて通過済み)からRMSとピークを測り `uiOutRms/uiOutPeak` へ。ballisticsは**時間定数**(RMS attack 20ms/release 250ms、peak 450ms減衰)なので**バッファサイズ非依存**。表示は-60〜+6dBの横バー+ピークマーカー、-6dB超でアンバー、0dBFSでピンク。⑥**共通化**: プリセットの読込は `voxMorphApplyPreset()`(1 Undo・ロック尊重・欠落項目はデフォルト)へ切り出し、PRESETSタブと共用。BETA/Audio Settingsウィンドウ用に `ParamRow`(数値欄・↺・🔒の3点セット、ロック状態を4Hzで自己同期)を新設=**別ウィンドウでもロック機能が失われない**。**注意**: Auto-Muteはスタンドアロン専用ウィンドウへ移ったため、DAWプラグインのUIからは操作できなくなった(元々DAWでは動作しない機能なので実害なし。パラメータIDは不変)。検証: `dsp/` 無変更のため offline_test は非実行。macOS/clang で AU/VST3/Standalone 3パターン相当の構成を `-fsyntax-only -Wall -Wextra -Wshadow` で通し、新規警告ゼロを確認(残る警告はすべて既存)

- **v0.29.2: Matchingのイントネーション設計見直し + TargetキャラクタークリックでFileChooserが開くUIバグ修正 + Air/AirShine自動設定**。①**UIバグ**: TargetFile選択後に他のキャラクターを選ぶとファイルダイアログが開いた。原因は**JUCEのラジオグループが他ボタンを`setToggleState(false, sendNotification)`で消すため、消される側のonClickも発火する**こと。TargetFileのハンドラが「選ばれた」ではなく「外された」時にも走っていた。対策=各onClick冒頭で`getToggleState()`を見て、ONにされた時だけ処理する(キャラクター側にも同じガードを入れた。そちらは逆向きの誤発火を起こしていた)。②**イントネーション**: エンジンの式は `f_out = center * ((f_in * 2^(pitch/12)) / center)^range` なので、**ピッチの着地点とpivot(center)がズレているとその差がrange乗で増幅される**。旧実装は pitch が目標より1st下を狙う(kPitchBias)のに center は目標ちょうどに置いていたため、**1stのズレがrange倍**になっていた。実測(あんじゅ): 目標f0=276.1Hzに対し変換後中央値246.0Hz(**-2.00st**)、Amountは**200%に張り付き**。対策=**pitchとcenterの両方を同じ「バイアス済み目標中央値」に向ける**→シフト後の中央値がpivotと一致し、**中央値がrangeに対して不変**になる(offline_testで5ケースとも誤差0.000st)。あわせて**kRangeMax=140%**(ユーザー指定)、**kRangeBoost 1.15→1.0**(この係数は上記のたるみを補正するためのものだったので、たるみを直した今は単なる過剰イントネーション)。実測: あんじゅで Amount 200%→**140%**、中央値誤差 -2.00→**-1.00st**(=意図した知覚バイアスそのもの)、KITUNE_targetでspreadが目標4.08に対し4.69→**4.08**。③**Air/AirShine自動設定**: 息成分は上位帯域のHNRの低さに出るので `air = 0.06 x (自分のHNR - ターゲットのHNR)` を0〜0.6に、`airshine = 0.35 x (>6kHz相対レベル差)` を0〜3dBにクランプ(いずれもパラメータ上限1.5/6dBの半分以下。テクスチャは行き過ぎる方が害が大きい)。**負にはしない**(「負の息」は無いので、ターゲットの方がクリーンなら0が正解)。**内蔵ターゲット/旧.vmprofileはHNR未計測なので、ゼロ既定と比較すると「無限に息っぽい」と誤読する→両者が計測値を持つ時だけ設定する**(`airApplied`)。実測ではユーザー録音のHF帯がノイズで相対的に強いため、3ターゲットとも air=0/shine=0(=余計なことをしない、安全側)。**新テストデータ**: あんじゅ(中性声)を `VoxMorph_share/あんじゅ{1,2}.m4a` で受領。**m4aは `afconvert -f WAVE -d LEI16@44100 -c 1 in.m4a out.wav` で変換できる**(ffmpegは未導入)。あんじゅ実測: f0=276/265Hz、spread=6.43/6.59st(非常に表情豊か=これがAmount 200%張り付きの原因だった)

- **v0.29.1: 解析器の包絡・追跡をリアルタイム経路(PsolaEngine)の方式に統一**。ユーザー指摘「フォルマント検出はメインタブのリアルタイム変換でも使っている。壊さないように、その設計をそのままコピーして使え」を受けての修正。**`dsp/PsolaEngine.h` と `src/PluginProcessor.*` は1行も変更していない**(diff 0行を確認。変更は `dsp/VoiceAnalyzer.h` と `test/offline_test.cpp` のみ)。**エンジン方式が優れている理由(実測で確認)**: ①包絡は**倍音1本につき1点を取り対数領域で直線補間**→構造的に倍音を追えない(ケプストラム次数を調整して回避しようとしていた問題が原理的に起きない) ②帯域内に内部極大が無ければ**前フレーム値を保持**(区間最大は減衰包絡では帯域端を返す=v0.28.3の「F1=1.35×f0」退化の正体。エンジンは元からこのバグが無い) ③**0.7/0.3のフレーム間平滑**。**決定的な実測**: エンジン自身のtrackF[]をユーザー録音で読み出すと F1/F2/F3 = 738/1672/2643(KITUNE_original)で、当方の解析器(717/1679/2970)とほぼ一致=**この録音でも情報は取れている**。差が出るのはターゲット側で、エンジンは F1=677Hz(妥当)、旧解析器は 269Hz(自分の基音352Hzより下=破綻)。**もう1つの自作バグ**: 探索範囲を話者ごとの推定声道長で スケールしていたため、**測ろうとしている差そのものを自己正規化で打ち消していた**→全話者共通の固定範囲(F1 200-1200 / F2 700-3400 / F3 1700-4600 Hz、アニメ声のF2が3kHzを超えるのでエンジンより広め)に変更。**HNRはゲートから重みへ**(kHnrFloor=0.3。ユーザー録音のF2帯はHNR +1.0dBしかないが、エンジンと当方が独立に0.1st以内で一致するので実構造はある。密度(identifiability)だけは引き続きハードゲート)。**結果**: ユーザー実データのグローバルFormantが **-2.00st → +0.67st(正)**。公開実測データ(Yazawa & Kondo)ベースの男→女/アニメ5ケースは**全て正値**、誤差 0.35/0.41/0.43/0.63/2.50 st。**ロボタイズによるピッチ正規化案(ユーザー提案)は実測の結果不採用**: 352Hz→100Hzへ1.8オクターブ下げるとターゲットのF3が2963→2064Hz(探索下限付近)へ落ち、F3帯のHNRも+2.2→+0.9dBへ悪化。PSOLAの大幅下降は包絡を復元するより先に滲ませる。**既知の弱点**: ①フレーム間平滑が母音境界をまたぐため母音別検出は5/5→3/5に低下(母音を落とす方向であり、誤った値を出す方向ではない) ②声道×1.20・f0=380の極端なアニメ声で誤差2.50st(正値だが過小)。**offline_testの「global formant recovery」は判定から診断表示に降格**(男性表を定数倍した合成は物理的に不自然=男性フォルマント×1.30でf0=352の声は実在しない。判定基準は公開実測データを使う男→女/アニメブロック側)

- **v0.29.0: Matchingを母音単位・信頼度重み付きへ全面刷新(ユーザー報告①②への対応)**。**①フォルマントがターゲットに一致しない問題の根治**。v0.28.4は「両録音の母音内容が違う」と結論して警告を出す方針だったが、今回**合成母音(既知フォルマント)によるグラウンドトゥルース評価**を導入して測り直した結果、**推定器そのものが主因**だと判明した。(a)旧包絡は「±0.8倍音間隔の移動平均」で、箱型平均は倍音リップルを19%残す(sinc(1.6))ため、**高ピッチ声では極大値が倍音そのもの**になる。実測: f0=352Hzで**F1誤差3.3st / F2誤差4.8st**。(b)旧探索は250-1000/850-2600/1900-3800Hzの**固定・非重複3窓**で、実在の母音がその境界をまたぐ(/u//o/のF2は800Hz付近=F2下限850未満)ため**帯域端に張り付く**。→ **真の包絡(反復ケプストラム)+候補同時割当**に置換し、同条件で1.4st/2.4stへ改善。**(c)最重要**: **フォルマントの位置は倍音が疎だと同定不能**(「F1が無い」でも「F1が基音より下」でもない — 位置が特定できないという意味)。有声音のスペクトルは声道共鳴をf0の整数倍でしかサンプリングしないので、f0=352Hzのターゲット(まきら)では352/704/1056Hzの3点しか無く、**第1共鳴が250〜500Hzのどこにあっても同じように見える**。どの推定器も「見つかる最低のもの」を返して**大きな正のバイアス**を出す。なお**F0>F1自体は物理的に有り得る**(音源=声帯振動と フィルタ=声道共鳴は独立。ソプラノが狭母音のF1より上で歌うのが実例)。**このターゲットのf0=352Hzはオクターブ誤りではない**ことを検証済み(YIN非依存の素の自己相関で60/60フレーム一致、176Hzの線は352Hzより31.9dB下=存在しない)。ユーザー(袴さん)から「F0の下にF1があるのは理論的に変では? まきらはF1のゲインが大きいのでは?」と的確な指摘を受けて検証した経緯。実測H1-H2はターゲット+10.8dB / ユーザー声+19.2dBで、低域が強いこと自体は事実だが、**F1の位置は特定できない**(H1-H2は音源傾斜と共鳴が交絡するため指標にならない)。→ `rel[3]`(F/f0から**値を見る前に**算出)で重み付けし、測れない帯域は個別補正せず**グローバルFormantに委ねる**(声道長が違えば全フォルマントがほぼ同一倍率でスケールするので、これが物理的に正しい)。**「出力値を見て異常判定するガード」は過去2回とも撤回済み(v0.28.3/v0.28.4)なので、今回は発生源の信号特性で判定する方式に統一した**。**②AEIOU母音別マッチング**: 各録音を母音別(A/I/U/E/O)に集計し、**同じ母音同士**を比較。グローバルFormant=全(母音×帯域)比較の**加重中央値**(重み=rel×フレーム数×帯域VTL信頼度`kVtlW={0.29,0.29,1.0}`。F1/F2は母音間で5.9st(sd)動くのに対しF3は1.7stしか動かない=残留する母音ミスマッチに3倍強い)。残差は帯域別トリム(kBandTrust=0.5で縮小、±2stクランプ)と**母音別残差→`va_*_f{1,2,3}`/`va_*_g{1,2,3}`へ書き込み+vcharacter=Custom/vadapt=ON/vamount=100%**。**実測(ユーザー実データ)**: f1shiftが**+3.00st(クランプ張り付き)→ 0.00**、グローバルFormant -2.68→+0.54st、母音一致4個・ばらつき0.40st。**合成グラウンドトゥルース(offline_test「Matching」節)**: 内容が揃った5ケースの最大誤差**0.15st**(旧方式は同条件で最大3.79st)、内容が食い違う2ケースは誤差2.2stだが**両方ともlowConfidenceで自己申告**する(kMadWarn=2.0st。良ケースのMADは0.08-0.37st、実データ0.40st、破綻ケース2.38stの間に線を引いた)。**運用上の要点は不変**: With target playで**同じ内容を読み上げる**のが最も効く。**互換性**: `.vmprofile`は`r1-r3`属性と`<VOWEL>`子要素を追加(旧ファイルはrel欠落=1.0扱いで従来通り読める)、内蔵ターゲットカタログは`rel={1,1,1}`を明示(**新フィールドのゼロ初期化を放置するとカタログ全体が信頼度0で破棄されるので注意**)。内蔵ターゲットは母音表を持たない(録音が無く、捏造した数値をAEIOUマップに入れないため)ので**設計上グローバル経路**を通り、この場合は警告を出さない(`lowConfidence`は「両者が母音表を持つのに一致しなかった」時のみ)。**教訓: 推定器は必ず既知の正解データで測ること。v0.28.3/v0.28.4の2回の迷走は、推定器の精度を一度も測らずに下流の症状だけを見ていたことが原因だった**

- **v0.28.4: v0.28.3のF1ガードを撤回 + プロファイル非互換の警告表示**。ユーザー実データ(MyVoice: f0=146.2/F1=366/F2=1529/F3=2885/tilt=34.05、TargetMakira: f0=352.0/F1=549/F2=1001/F3=2283/tilt=20.40)で症状を**完全再現**し、真因を確定した。**帯域ごとに必要なシフト量が F1 +7.02 / F2 -7.33 / F3 -4.06 st とばらばら(スプレッド14.3st)**。単純平均のグローバルFormantが**-1.46st(負)**になり、その結果 f1shiftが**+3.00st(±3クランプに張り付き)**でもF1は366→400Hzまでしか届かない(目標549)。F0だけは146→332Hz(目標352)で一致 — ユーザー報告と完全一致。**物理的な原因**: 声道長が違えばF1/F2/F3は**ほぼ同じ倍率**でスケールするはずなのに、この2プロファイルは F2/F1比が MyVoice=4.18(前舌/i//e/)、Target=1.82(円唇/o//u/)と**母音の中身が違う**。加えてターゲットのf0=352Hzは非常に高く、F3以下に倍音が7本程度しかないためF2/F3の推定自体が不安定(F2=1001Hzは倍音3次1056Hz付近を拾っている疑い)。**この算出はv0.27.0と同一で回帰ではない**(v0.28.3のF1ガードはこのデータでは発火しない: 549 > 1.5×352=528)。**対応**: ①**v0.28.3のF1ガードを撤回**(`F[0] < 1.5×f0`判定)。実在する低いF1(f0=280の明るい声のF1=380Hz等)で誤発火し、**グローバルFormantを+3.4stも動かしてF2/F3ごとずらす**ことを実測確認したため。数式はv0.27.0と完全同一に戻した。フォルマント推定の破綻は**発生源(VoiceAnalyzer)で直す**方針に統一。②**`MatchingEngine::Proposal`に`bandShiftSt[3]`/`bandSpreadSt`を追加**し、スプレッド>6stならMATCH後のステータスに「⚠ F1/F2/F3 +x.x/+x.x/+x.x st — 3帯域の要求量がばらばらです(録音内容が違う可能性)。ターゲットを再生しながら同じ内容を録音し直すと精度が上がります(With target play)」と警告。黙って無意味な平均を適用せず、ユーザーが原因に到達できるようにした。③ステータス欄の高さを24px固定から`jlimit(24,76,h/3)`へ(MATCH後は複数行出るため従来は見切れていた)。**運用上の要点**: Matchingは2つの録音の**中央値**を比較するため、**両者の音韻内容が揃っていること**が前提。With target play(v0.28.3で既定ON)で同じ内容を読み上げるのが最も効く。v0.28.3のVoiceAnalyzer極大値修正と内蔵ターゲットtilt修正はそのまま維持
- **v0.28.3: F1がターゲットF0へ収束するバグの原因究明と修正 + MyVoice UI追加**。ユーザー報告「マッチング実行時にF0とF1の変換先が両方ともターゲットのF0に収束する(v0.27.0では未発生)」の調査結果と対策。**調査結論: Auto-Setの数式はv0.27.0(9c51960)とバイト単位で同一**(`git show 9c51960:src/PluginEditor.h`のapply()と`dsp/MatchingEngine.h`のautoSetを比較確認済み)。内蔵ターゲット経由の全経路も数値検証で正常だった(男性合成音120Hz→feminine_standard: pitch+8.8/formant+3.46、エンジン通過後の実測 出力f0=202/F1=621、F1の収束なし)。**真因は`VoiceAnalyzer`のフォルマント探索**: F1は`max(250Hz, 1.35×f0)`から上を探索するが、ピーク検出が「区間内の単純最大値」だったため、**包絡がその帯域で単調減衰する場合(高いピッチの声で真のF1が探索下限以下にある時など)、常に探索下限そのもの=1.35×f0がF1として返る**。するとターゲットのプロファイルが「F1はf0の約1.4倍」と主張し、Matchingは忠実にユーザーのF1をそこ(=ターゲットF0のすぐ隣)へ寄せる=報告された症状そのもの。**v0.27.0で顕在化しなかった理由**: 当時はEstimated(変換先)系列がグラフに無く、この収束が可視化されていなかったため。**対策①`VoiceAnalyzer`**: フォルマントを「区間内の極大値(env[k]>=両隣)」として探索し、極大が無い場合のみ従来の区間最大へフォールバック(PsolaEngine::spectralProcessのフォルマント追跡と同じ規則に統一。offline_testの解析器出力は656/1254/2578で変化なし=非破壊)。**対策②`MatchingEngine::autoSet`にガード**: どちらかのプロファイルで`F[0] < 1.5×f0Hz`(=8半音未満)なら測定失敗とみなし、`sh[0]`をF2/F3のシフト平均で置換(F1だけを引きずり下ろさない)。実測: 退化ターゲット(f0=261,F1=375)に対し**変換先F1が434Hz→582Hzへ改善、f1shiftも-2.55→0.00**。**対策③内蔵サンプルターゲットの`tiltDb`スケール修正(v0.28.0で私が入れた欠陥)**: VoiceAnalyzerのtiltは`10*log10(E60-1000/E2000-8000)`で音声では大きな正値(合成母音で+8.45、実声はさらに上)なのに、-1.5〜+0.5という別スケールの値を書いていた。結果Auto-Setの`0.25×(target-mine)`が常に-4dBクランプへ張り付き、どのターゲットを選んでも最大限ブライト方向へ振れていた。実測スケール(+9〜+17、相対順序のみが意味を持つ)へ修正 → 同条件でtiltが-2.49→+1.14の穏当な値に。**UI追加**: ①MyVoiceセクションのSave Profile隣に**Reset All to Defaults**ボタン(PRESETSタブと同じ挙動=全パラメータをデフォルトへ、1 Undo、ロック項目は保持。押下後はnSet/nLockedもクリアしてEstimated再計算)。②**With target play を既定ONへ**。これに伴い`startPlayForCapture()`を「再生できる音声が無ければ何もしない」no-opに変更(従来は再生不可時に**録音自体を中断**していたため、音声を持たない内蔵Character選択時に既定ONだとRecordが機能しなくなる)
- **v0.28.2: Matching / MyVoice UI 修正(ChatGPT VoxMorph_v0281_Matching_MyVoice_UI_Fix_Instructions、4コミット分割)**。①**4cb41ff fix(matching): make target selection explicit and start unselected** — `selectedSampleIndex=-1`初期化、`clearTargetButtonSelection()`/`selectOnlyTargetButton()`ヘルパを追加、選択変更は必ずこれを経由(radioGroupだけに頼らない)。`selectSampleTarget`から`fromUser`引数を削除、`restoreLastNonFileSelectionUi`→`restoreTargetSelectionUi`にrename(未選択ケースはclearTargetButtonSelectionにfallback)。ctorで`selectSampleTarget(0,false)`を削除し**起動時は全ターゲット未選択・MATCH disabled**、prof2={}/lastTarget={}/p2Lbl="Target: --"にリセット。FileChooserキャンセルパスにもrestoreTargetSelectionUi()呼び出し追加。②**332f419 feat(matching): add square MyVoice record and file tiles** — `TargetCharacterButton`を`VoiceTileButton`に一般化(`TileIconKind::{character,file,record}`enum、`clickingToggles`パラメータ、恵子TextButton::TextButton(label)を継承してsetButtonText経由で描画=recording中の表示更新が効く)、`TargetCharacterButton`を型aliasとして温存。`recBtn`を`VoiceTileButton`(record iconキ=赤ドット)、新規`myVoiceFileBtn`(file icon)、両者をTarget tileと**同じsizeで並置**(共有tileSize=`std::clamp((width-gap*(cols-1))/cols, 72, 96)`)。"Recording... speak!"→"REC..."(square幅に収める)。MyVoiceFile onClickは次コミットまでstub。③**48729cc feat(matching): load and save MyVoice profiles** — `loadFile`→`loadTargetFile`にrename、新規`loadMyVoiceFile`+`applyMyVoiceProfile(profile, sourceName)`共通化(録音完了+ファイル読込+.vmprofile読込の3経路をここに合流。nSet/nLocked reset+refreshEstimated+graph.repaint+updateMatchStatus)。共通decode helper=`decodeMonoForAnalysis(file, sr, maxSec)`(reader guard/nOut<=0チェック/線形補間モノ化を1関数化、`DecodedMono{samples, sampleRate, error}`を返却)。Target loaderは**ローカルvectorで解析成功後にproc.prevBufへコピー**する2段階に変更(解析失敗時に既存Target previewを破壊しない=spec 11.2)。**MyVoiceFileはproc.prevBuf/prof2/lastTarget/Target選択に一切触れない**(spec 5.6)。FileChooser を3本に分離(`targetChooser`/`myVoiceChooser`/`profileSaveChooser`)、`saveTargetProfile`/`saveMyVoiceProfile`は共通`saveProfileImpl`経由(**snapshotをlambda captureして非同期中のprof1/prof2書換に耐える**=spec 6.2/11.3)。UI=MyVoice options行に`saveMyVoiceProfBtn`(With target playの次、spec 6.3)、両save buttonはprofile.valid()でenable/disable。timerCallbackは録音失敗時もクリアなstatus表示。④**(本コミット) fix(matching): move graph below actions and clear stale status** — updateMatchStatus()中央文字を「RECORD MYVOICE」→**canMatch=trueなら"READY"、falseなら空文字**(disable状態の見た目自体で伝わる)。`invalidateForTargetChange()`で`nSet=nLocked=0`にresetして古い"N APPLIED"がstaleにならない。AutoMatching resized()を**上から順**にレイアウト変更(見出し→MATCH/SAVE PRESET actionRow→save editor(必要時)→graph→status)、旧来のremoveFromBottomでの逆順予約を撤廃=**視覚的にボタンが先・グラフが下**へ。バージョン v0.28.2 バンプ。DSP・APVTS・Undo/Lock・PresetPanel::visibilityChanged(v0.28.1)は不変
- **v0.28.1: Matching UI 修正(ChatGPT VoxMorph_v028_Matching_UI_Correction_Instructions.txt、3コミット分割)**。UIの見た目/操作性の指摘を反映、DSP・MatchingEngine・APVTS・Undo/Lock・録音経路は不変。①**Commit c5e4e17 fix(matching): simplify sections and remove phase rail**: 画面最上部の "01 CURRENT / 02 TARGET / 03 MATCH" rail + iteration counter を完全撤去(railCurrent/railTarget/railMatch/railIter/initRail lambda/resized・updateMatchStatusから参照)。"IN" srcChip も撤去。**MATCH AGAIN撤去**: refineBtn/doRefine/waitingRefine/applyProposalのisRefine分岐/iteration/profC/pCLblを削除、graph.conv=nullptr。ProfileGraph legendを条件表示に(null/invalid系列はスキップ、Matchedの空凡例が残らない)。セクション見出しをASCII限定に統一・順序を **TargetCharacter → MyVoice → AutoMatching** に変更(見出しは const char* の日本語混在文字化けリスクを回避、CMakeにMSVC /utf-8 は既存)。Record Current→**Record**へ改称。MATCH/SAVE PRESETをAutoMatchingセクションへ移動、matchStatusを中央配置。ファイル読込パスで解析失敗(voicedFrames < 15)を明示的に拒否・prof2を上書きしない。**MatchingEngine::refine()はヘッダに温存**(将来再利用・API互換)。②**Commit 0694a98 feat(matching): replace target dropdown with character buttons**: 新規`TargetCharacterButton`(TextButton派生、正方形72-96px、paintButtonで上60%=icon placeholder+下=labelを自前描画、選択/非選択で塗り+アウトライン両方が変わる)。ComboBox `sampleBox` + `loadBtn` を撤去し、5内蔵ターゲット+`TargetFile` の**6タイル**をshared radio group(kTargetRadioGroup=0x564d01)に配置。レスポンシブレイアウト: 幅≥650pxで6×1、それ以外で3×2。**FileChooserキャンセル/読込失敗でTargetFileへ誤選択されない**ように、onClick先頭でrestoreLastNonFileSelectionUi()を呼び直前の選択に一旦戻し、chooserコールバックの成功パスでのみselectTargetFileButton()を呼ぶ。auxiliary Play/Save Profileはgridの直下に短い行として残置。③**Commit(本) fix(presets): refresh list after Matching save**: PresetPanel::visibilityChanged() overrideを追加、isVisible()時にrefreshList()を呼ぶ。これでMATCHINGタブから保存→PRESETSタブに切替えた瞬間に新規プリセットが一覧に出る(JUCEはタブ切替でsetVisibleを行うため)。加えてMatchingPanel::savePreset()はwriteToに加えfile.existsAsFile()を確認、成功時ステータスに「(PRESETSタブで確認)」ヒントを追記。旧`sampleBox`関連の`loadSampleTarget()`は`selectSampleTarget(int, bool)`に一般化、`targetFileActive`/`selectedSampleIndex`で状態管理
- **v0.28.0: Matchingタブ新設(Phase 1、ChatGPT指示書=VoxMorph_Matching_UI_Design_Spec.txt / Steps 1〜7)**。従来のANALYZEタブをMATCHINGタブへ改称・再設計。Phase 1の目的は「音を変えないUI移植」。①**タブ名変更**: ANALYZE→MATCHING。②**新規`dsp/MatchingEngine.h`**: 依存ゼロ・JUCE非依存の純粋計算ヘッダ。autoSet/refineの式・jlimitレンジ・kPitchBias/kRangeBoost・damping係数はv0.27.0のAnalyzePanelからバイト単位で同一(byte-exact移植)。加えて`predictEstimated(profile, paramGetter)`で現在パラメータからのEstimated予測(spec 7.3、DSP実行なしの単純モデル)。Proposal構造体で最大16 changeまで固定サイズ配列に返す(音声スレッド安全性は関係ないがno-alloc)。③**新規`dsp/SampleTargetCatalog.h`**: 5つの内蔵ターゲットVoiceProfile(Feminine Standard/Bright/Warm/Youthful/Androgynous、統計値のみ・音声ファイル非同梱)。stable id・表示名(英/日)を持ち、初期選択=Feminine Standard。④**MatchingPanel(旧AnalyzePanel全面書き換え、~430行→~470行)**: レイアウトを**Current First**へ(spec 2.3): 上=[Record Current][録音時間][With target play][IN chip]、中=[Sample▼][Load Target File...][Play][Save Profile...]、下=[MATCH][MATCH AGAIN][SAVE PRESET]+correction/iteration status。ボタン文言:「Auto-Set Parameters」→「MATCH」、「Record Converted + Refine」→「MATCH AGAIN」、iteration counterをsession railに表示。MatchingEngineを呼び、既存のhistory.group+setP(isParamLocked)経由でパラメータ書込(1 Undo/Match、Locked尊重)は不変。Currentは`proc.capFromOutput=false`(INPUT PIPELINE、v0.27相当)のまま=spec 2.4のCaptureSource切替(OUTPUT+residual戦略)はPhase 2以降。Sample選択切替時はprof2差替+Matched破棄+iteration=0(spec 6.2「ターゲット変更」)。⑤**ProfileGraph拡張**: 系列が4本(Current=ミント●/Target=金○/Estimated=紫◇+破線/Matched=ピンク◎)に、形状glyphで色覚差非依存の識別を実現(spec 4.4)。Correction ◤◢ hatch帯(Correction Cyan)をCurrent↔Estimated formant位置間に描画(spec 4.5)、方向は補正量の符号を示す。Legendも glyph 付きに更新。Estimatedはpaint前にMatchingEngine::predictEstimatedで算出。⑥**Save Preset**: MatchingPanel末尾のインライン名前入力+Saveで`<userAppData>/VoxMorph/Presets/名前.vmpreset`へ通常のAPVTS state XMLを書出(PresetPanelのsave()と同形式=**別concept化しない**、spec 1.7)。⑦**互換性**: APVTS parameter ID/範囲/default不変、.vmpreset/.vmprofile読込互換、lockedIds継続、deprecated airband/air2/air2low温存、MAINのリアルタイムSpectrum不変。**Phase 2以降(未着手、次コミット候補)**: VoiceAnalyzer::analyzeDetailed(時系列/confidence/voiced mask)、GraphCard 7 renderer分離(Ring/Band/Timeline/Density/Scatter/Arc/Matrix)、MatchingViewModel/phase enum、CaptureSource OUTPUT+residual戦略、Parameter Registry、AEIOU/Natural Air等をReference Only表示、preset metadata、責任分割(src/ui/matching・src/matching・src/presetsサブディレクトリ)、responsive breakpoint、accessibility summary、Reduce Motion。**受け入れ条件Section 19の1〜13は満たし、14(狭幅Viewport)・15の一部(async解析)は未対応**
- **v0.27.0: AEIOU Character Formant Gain実装(ChatGPT指示書、正式実装)**。既存のシフト補正に加え、母音ごとのF1/F2/F3相対共鳴ゲインをプリセット化して同時適用。①`AEIOUCharacterMap`に`gainDb[5][3]`を追加、8内蔵キャラ全て+CustomがNaturalベースの15値を持つ(Naturalは控えめ、Active/Loli/Anime/Lilyは±3dB近い強めのF2/F3ブースト)。②`VowelAdaptiveWarp`にゲイン経路(mapGain/offG)を追加、シフトと同じ連続母音座標・アンカー補間・confidence gate・attack/release smoothingを共有(重み計算とgateを共通化して1回だけ)。値レンジ=マップ入力±3dB(kMaxMapGain)、Amount適用後の最終上限±4.5dB(kMaxGain)。③エンジン側の適用点=`spectralProcess`の既存フォルマントピークガウス加算部で`fEffGain[i]=fGainDb[i]+vaHiScale*vaw.gainDb(i)`(手動F1-F3 Gainに加算、High Rangeガード連動)。④**軽いエネルギー補正vaComp**=`-0.6*(0.5*g[0]+0.3*g[1]+0.2*g[2])`(F1が主エネルギー寄与、±2dBクランプ)を全ビンへ加算、母音ごとの総音量差が過大にならないように保護。⑤新規APVTS=`va_{a,i,u,e,o}_g{1,2,3}`(±3dB step 0.1、既定Nat値)を15個追加、Custom時のみ読取。⑥DETAILウィンドウ拡張=セクションラベル「Formant Shift (st)」「Formant Gain (dB)」で2ブロック(5×3スライダー×2)、ウィンドウを700×640に拡大、内蔵Character選択時は両方read-only、Custom時のみ両方Attachment、Copy to Custom/Reset Customはシフト+ゲイン30値を1 Undoステップで書込み。⑦Amountはシフト・ゲイン両方に連動(共通の`gate`から派生)、100%=プリセット値そのまま、200%=2倍(kMaxGain=4.5dBで頭打ち)。off/Amount 0はbit-identical(gate=0でoff/offG両方0)。**注意**: vcharacter enum等は変更なしのため保存セッションは互換、Customを使っていた場合はゲイン欄がNat初期値で追加される。既存の`va_*_f*`パラメータ・IDは不変
- **v0.26.2: プリセット全面改訂+「Lily(百合声)」追加(ChatGPT指示書、キャラ間コントラスト明確化)**。DSP変更なし、`dsp/AEIOUCharacterPresets.h`の値のみ更新+8番目の内蔵キャラLilyをAnimeとElegantの間に挿入(enum/StringArray/パネルnames[]の3箇所同期、custom==lastの前提はコード全体がenum参照のため無事)。新値の傾向: Soft/Elegant/UniはマイナスF1-F3方向を使い女性化と分離、AnimeはI/EのF2を+2.85〜3.00まで振りA/OのF2をマイナスに(母音対比最大)、LilyはLoli系高共鳴だがF1控えめの澄んだ系。**注意: vcharacterはAudioParameterChoiceのためitem追加で正規化値の意味が変わり、v0.26.0/0.26.1で保存したElegant/Uni/Custom選択は読み込み時に1つずれる可能性あり**(リリース直後の個人利用のため許容、以後のitem追加も同様の影響が出る点に留意)
- **v0.26.1: AEIOU Character効き強化+DETAIL配色修正(ユーザー評価「Character間の差・Amountの効きが微差」への対応)**。①vamountレンジ0-100%→**0-200%**(既定60%のまま。APVTS状態は実数値保存のため旧セッションの値は不変。ホストの正規化オートメーションレーンだけは0.5=100%に意味が変わる点は許容)。エンジン側クランプも0..2へ。②VowelAdaptiveWarpの**最終オフセット上限をF1±3/F2±4.5/F3±2.25 stへ拡大**(kMaxOff。100%超でも各母音が同じ天井に張り付いてキャラ差が消えないように。手動F1-F3シフトが±6 stまで実績があるためエンジン的に安全域)。マップ入力値のクランプは従来どおり±2/±3/±1.5(kMaxMapに分離、Customパラメータレンジも不変)。③DETAILウィンドウの白文字問題: DocumentWindowはeditorのLnFスコープ外でJUCE既定ダークテーマになっていた→AEIOUCharacterPanelに専用LookAndFeel_V4(ライト、メインと同じミント基調・本文/数値0xff2e2e32)を設定+sendLookAndFeelChange()(数値欄はLnF適用前生成のため必須、v0.10.0と同じ罠)。デストラクタでsetLookAndFeel(nullptr)
- **v0.26.0: AEIOU Character(母音適応ワープの完成版、ChatGPT指示書一括実装)**。v0.25.0のBeta機能を正式機能化・改名(「Target」は使わない=特定人物再現ではなく母音共鳴キャラクター作りのため)。**内蔵プリセット値の定義場所=`dsp/AEIOUCharacterPresets.h`の1箇所のみ**(enum AEIOUCharacter{natural,soft,active,loli,anime,elegant,uni,custom}+getAEIOUCharacterMap()、[A,I,U,E,O][F1,F2,F3]st、DSP/UI/Customデフォルトが全てここを参照)。7キャラ=Natural(自然な女性声)/Soft(柔らかい)/Active(元気)/Loli(幼な声、40-70%想定)/Anime(母音間コントラスト誇張)/Elegant(お姉さん)/Uni(中性)。**パラメータ**: 既存vadapt/vamountのID維持(表示名のみAEIOU Character/AEIOU Amountへ)、新規vcharacter(AudioParameterChoice 8択、既定Natural)+Custom15値 va_{a,i,u,e,o}_f{1,2,3}(F1±2/F2±3/F3±1.5st step0.01、既定=Natural値)。**vamountの既定を0→60%へ変更**: 安全性はvadapt既定オフが担保(v0.25.0保存セッションは自身のvamount値を復元、それ以前のセッション/プリセットはvadaptも欠落=オフなので音は不変)。**DSP経路**: PsolaEngine::Params::vowelMap(AEIOUCharacterMap)をprocessBlockで解決(Custom選択時のみ15 raw値、それ以外は内蔵テーブル参照=文字列/動的確保なし)→setParams()でvaw.setMap()(クランプ付き15floatコピー、制御レート反映)。Character切替も既存attack/releaseスムージング経由でジャンプなし。engine/engineR両方へ同一Params(ステレオ同期)。**UI**: FORMANTセクション=ON/OFFトグル+Characterプルダウン(新規ComboRow/addComboRow、リセット・ロック対応)+Amount+DETAIL...ボタン(新規ButtonRow/addButtonRow)。**DETAIL別ウィンドウ**=AEIOUCharacterWindow/AEIOUCharacterPanel(620×420、PluginEditor.h内、FxChainWindowと同パターン: editorのunique_ptr所有=親破棄で消滅、再クリックはtoFront)。5母音×3スライダー: Custom選択時のみAPVTS Attachment+編集可(ロック中パラメータはdisable)、内蔵キャラ選択時はattachment解除+値表示のread-only。Copy to Custom=15値コピー→vcharacterをCustomへ(history.group=1 Undoステップ、ロック項目スキップ)。Reset Custom=Natural初期値へ(確認なし)。キャラ切替追従は4Hzポーラー。デバッグgetterのthread-safeコメントは「UIから読むならatomic経由に」へ修正(現状UI未使用)。テスト: 既存VA節全PASS+ゼロマップ→オフセット0のプラミング確認追加。実声評価・実機ビルド確認は未了
- **v0.25.0: 母音適応型フォルマントワープ(Vowel-Adaptive Formant Warp、Beta)**。ChatGPT設計指示書ベース。既存のF1/F2/F3個別ワープ(spectralProcessの区分線形包絡ワープ)を置換せず、**入力の母音状態に応じた動的オフセットを手動Shiftに加算**する構造。新規ヘッダ `dsp/VowelAdaptiveWarp.h`(依存ゼロ、制御レート=512サンプル毎、状態はfloat数個でalloc/ロック/例外なし): ①trackF[0..2]をfrSmで割って入力ドメインへ逆スケール(trackFはリサンプル後コンテンツ追跡のため) ②連続母音座標 height=norm(log2 F1, 280..950Hz) / frontness=norm(log2(F2/F1), 0.55..2.90)(比率で話者スケール減感、hard classifyしない) ③5アンカー(/a i u e o/座標は日本語典型フォルマントから算出)の逆二乗距離ブレンドでF1/F2/F3オフセット(st)を生成 ④信頼度ゲート=amount×YIN明瞭度(0.15-0.5整形)×フォルマント移動速度stab(motSm/0.06oct)×妥当性(範囲150-1100/500-3200/1500-4500Hz・F1<F2<F3順序・有限値)、全て連続量 ⑤attack35ms/release90ms/無声・無効時150msで0へ(F1/F2/F3独立1-pole) ⑥最終クランプ±2/±3/±1.5st。適用点=spectralProcessのdBin計算(fEffRatio=fShiftRatio×2^(vaHiScale×offset/12))、**High Rangeガードの実効フォルマント縮小係数vaHiScale=1-wHi(1-hiFAmt)を同乗**。無声グレインはspectral自体が走らずConsonant Shift/Natural Air経路に不干渉。**ON時はスペクトル層を常時起動**(F1追跡が必要なため。CPUオフライン代理: baseline 0.99%→ON 1.71%=手動F1-F3使用時と同等、既にF1-F3使用中なら追加ほぼ0、OFF時増加ゼロ)。**内蔵マップは控えめな女声化サンプル**(最大1.2st: a={1.2,0.6,0} i={0,0.8,0.5} u={0.4,1.2,0.3} e={0.8,0.8,0.3} o={0.9,0.4,0})で、`setAnchorOffsets()`で差し替え可能=Phase 5(ターゲット解析からのマップ生成)のフック。パラメータ=vadapt(bool既定off)/vamount(0-100%既定0)、**off/0%はbit-identical**(テストで確認)。UI=FORMANTセクション末尾に(Beta)トグル+Amountの2行、英日ツールチップ。デバッグgetter=vowelHeight/vowelFrontness/vowelConfidence/vowelOffsetSemi。テスト追加(offline_test「Vowel-Adaptive Formant Warp」節、全PASS): bit-identical互換×3、/a/座標妥当性(h=0.75,f=0.10)+オフセット上限+レベル中立(-0.05dB)、/a/→/i/適応差(F1 +1.06→0.00st)+ブロック間ジャンプ<0.35st(実測0.072)+無声1秒で0、ゴミ入力(NaN/Inf/0/順序異常)有限性+0復帰、alloc 0+LowVoice/GCI/Air/LowLat共存。実声評価は未了

## 凍結中(ユーザー指示によりストップ)

- **Natural Air改善フェーズの数値追い込み(2026-07-18確定)**: v0.23.0を安定候補とし、ノッチ幅・ゲート強化による残差(+0.4〜0.8dB)のさらなる削減は停止。音色・副作用・息成分の保持を優先し現在の安全マージンを維持する。**air2lowを標準動作(トグル廃止)へ統合する条件**=次の実声条件で副作用なしをユーザーが確認後: ①通常の低音男性声 ②息混じりの低音 ③ボーカルフライ ④低音→中音グライド ⑤摩擦音を含む短文 ⑥発声の立ち上がり/減衰。**Low Voice Modeの位置づけ**=ゴースト除去機能ではなく低F0/フライ区間のピッチ追跡継続機能(フライ由来の低周期成分が出力に残るのは仕様上のトレードオフとして許容)。grainBlend/grainAvg/grainHalfPは実聴差なしのため既定化せず実験フックのまま保留

- Low Latency Mode の追加開発(実装済み・動作するが優先度下げ)
- Breath改良、専用仮想デバイス同梱
- GCI Grain Sync の追加チューニング(v0.7.1でBeta化・グライド退避を入れて凍結。主用途はLow Voice Mode併用の低音・フライ声)

## 次の候補(未着手)

- **Matching Phase 2〜4(v0.28.0の続き)**: (a)`VoiceAnalyzer::analyzeDetailed`追加=`VoiceAnalysisResult`(schemaVersion=2、F0/F1〜F3の`MetricSeries`(median/p10/p90/values/confidence)、voicedMask、frameTimeSec、AnalysisQuality)。既存`analyze()`は`analyzeDetailed().summary`のwrapperにして後方互換維持。(b)GraphCard基盤=`Spec/State/Renderer interface`+7 renderer(Ring/Band/Timeline/Density/Scatter/Arc/Matrix)、既存ProfileGraphは統合先。(c)MatchingViewModel+phase enum(spec 6.1、14 phase)、CaptureSource(input pipeline vs final output)+MatchStrategy(absolute vs residual)、iteration/session history(spec 6.2)。(d)Parameter Mapping Registry(spec 8.1)、AEIOU Matrix/Natural Air等をReference Only cardとして表示、実測されていない指標を実測に見せない(spec 8.C)。(e)preset metadata(createdBy/targetId/algorithmVersion/iterationCount/createdAt、後方互換確認後)、`.vmprofile` v2詳細(schemaVersion=2、詳細配列を子要素として、v1 readerは属性のみ読める)。(f)責任分割=src/ui/matching・src/matching・src/presets(現状はPluginEditor.hに集約。既存プロジェクト方針との兼合いでオプション)。(g)Responsive breakpoint(wide 900/normal 680/compact 480/narrow<480、GridまたはFlow layout)、Matching Viewport化(spec 14)。(h)Accessibility(keyboard focus、glyph識別、tooltipに単位/実値、AccessibilityHandler summary、最小hit target)、Reduce Motionフラグ。詳細=`VoxMorph_Matching_UI_Design_Spec.txt` Section 17〜18。

- **AEIOU Character Phase 5(測定ベースマップ)**: ANALYZEのMyVoice/Targetプロファイル対から母音領域ごとのF1-F3目標差を推定し、Custom 15値(va_*_f*)へ書き込む形で自動生成する(v0.26.0でCustom経路とhistory.group書き込みパターンが既に存在するため差分は小さい)。話者正規化・包絡カーブ補正はまだ着手しない。まずv0.26.0の実声評価待ち

- 音質向上の本命2つ(Mixed区間処理=v0.6.0 Air Preserve、GCI/ESOLA同期=v0.7.0 GCI Grain Sync)は実装済み。ユーザーの実声評価待ち
- **AIRセクション再設計(承認済み計画=`AIR_REDESIGN_PLAN.md`)**: Phase 1=Natural Air v2試作(v0.20.0で完了、実声評価待ち)。Phase 2=スペクトル側の倍音間ノイズ床測定による帯域別推定の補正(任意)。Phase 3=UIのAIRセクション化(air/airband/air2/breath2を「AIR」見出しへ移設、Air Preserve→「Natural Air」へ改名、Breathは「生成」・Natural Airは「保持」と役割明示、統合はしない)。Phase 4=実声評価→既定値決定→air2を既定オンにしてlegacy削除検討。実機CPU実測(AU/VST3/Standalone×複数バッファ)も未了
- Air Preserveの発展: 息成分へのフォルマント追従(現状は無変換バイパス)、実声での既定値チューニング
- **低音域・大幅アップシフト(+12st)のグレイン経路ゴースト(未着手・v0.22.0で切り分け済み)**: 低いf0(~106Hz)を+12stすると、air完全オフでも入力奇数次倍音が出力倍音の中間に-22dB程度残る(KITUNE_low実測)。原因はグレイン幅(1.25×出力間隔≒0.625入力周期×2)が入力1周期を超えて隣接パルスを含むこと+同一グレインの2回再利用。Natural Airでは解決不能。候補: 大アップシフト時のグレイン幅追加縮小(フォルマント平滑化とのトレード)、GCI併用の位相固定再利用、出力側スペクトルでの半グリッド抑制
- Breath改良の本命「連続ノイズ×声道フィルタ」には v2 の帯域別keep量(aB[4]=airBandAperiodicity())が「不足分だけ生成」の駆動信号として流用できる
- 専用スキンUI(ユーザーが完成イメージ画像を保有: サイドバーMain/Visualizer/Presets/Settings+カード式パステルデザイン。v0.9.0で配色とビジュアライザー、v0.12.0でプリセットまで実装済み。残りはカード式レイアウト化とアイコン類)

## アプリ名変更の手順(ユーザーが改名を予定。作業時はこの順で)

**推奨方針: CMakeターゲット名(VoxMorph)は据え置き、表示名だけ変える。** ターゲット名を変えると CMakeLists 内の target_* 5箇所と build.yml のartefactsパスも全て変更になるため。

1. `CMakeLists.txt`
   - `PRODUCT_NAME "VoxMorph"` → 新名。**これが表示名と成果物ファイル名(.app/.component/.vst3)になる**
   - `COMPANY_NAME`/`PLUGIN_MANUFACTURER_CODE` は任意。**`PLUGIN_CODE Vxmf` は変えない**(変えるとDAWから別プラグイン扱いになり、既存プロジェクトの設定が外れる)
2. `.github/workflows/build.yml`
   - codesign/cp の6箇所 `.../Release/{AU,VST3,Standalone}/VoxMorph.{component,vst3,app}` → 新PRODUCT_NAME のファイル名に(`VoxMorph_artefacts` フォルダ名はターゲット名なのでそのまま)
   - `--identifier com.hakamaaudio.voxmorph` は**変えると旧インストールに上書きされない**。変える場合は preinstall に旧identifier/旧ファイルの削除を残すこと
   - `VoxMorph-Installer.pkg`、Artifacts名 `VoxMorph-macOS-Installer` / `VoxMorph-Windows`(見た目だけの問題)
3. `scripts/preinstall` — 旧ファイル削除パス。**新名にしても旧名(VoxMorph.*)の削除行を残す**(移行のため)
4. `scripts/install-mac.command` — 中のパス類
5. `src/PluginProcessor.h` — `getName()` の "VoxMorph"
6. `src/PluginProcessor.cpp` `fxChainFile()` と `src/PluginEditor.h` `PresetPanel::presetDir()` の "VoxMorph" フォルダ名 — **変えると既存プリセット/FX設定が見えなくなる**。変えるなら起動時に旧フォルダから新フォルダへコピーする移行コードを入れるか、フォルダ名は据え置く
7. スタンドアロンの設定(オーディオデバイス・Cmd+S状態)はJUCEがアプリ名から自動命名したファイルに保存されるため、**名前変更で一度リセットされる**(仕様として了承を取るか、移行コードを書く)
8. ドキュメント: README.md / HANDOVER.md / DESIGN.md / 各ガイド、UI内の表記(PluginEditor.h のウィンドウタイトル等は現状ほぼ名前非依存)

## 引き継ぎ手順

新セッションで: このファイルと、必要に応じ `DESIGN.md` / `dsp/PsolaEngine.h` を読む。検証はサンドボックスで `g++ -O2 -std=c++17 test/offline_test.cpp` → `python3 test/analyze.py`(要numpy)。**`dsp/` を「音を変えずに」触る変更では、加えて `test/bitexact.cpp` で変更前後の出力を `cmp` すること**(使い方はファイル冒頭)。修正ファイルはユーザーに渡し、アップロード先フォルダを明記すること。
